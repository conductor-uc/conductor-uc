Deliberately empty. User/extension lookup is 100% dynamic, via `mod_xml_curl`
against telephony-config (see `../autoload_configs/xml_curl.conf.xml`) — this
node has no local registrar or user database (03 §1, D-007). The real
directory responses arrive in S1-13.
