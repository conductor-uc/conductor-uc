# pnpm keeps an isolated node_modules by default: a package may only import
# what it declares in its own package.json. Do not relax this.
strict-peer-dependencies=false
auto-install-peers=true
# The lockfile is committed; CI installs with --frozen-lockfile.
