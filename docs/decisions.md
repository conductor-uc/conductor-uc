# Decisions, open questions & gaps

Status values:

- **Accepted**: decided by the owner.
- **Proposed**: a recommendation made in these docs that needs sign-off before the stage listed in *Needed by*.
- **Open**: no recommendation yet, or the decision needs owner input.

## 1. Decision register

| ID | Decision | Status | Needed by | Where |
|---|---|---|---|---|
| D-001 | pnpm workspaces + Turborepo monorepo. Flutter app built separately in CI. | **Accepted** (2026-09-12) | S0 | [01 §5](architecture/01-system-overview.md#5-monorepo-layout) |
| D-002 | **The Master tier is completely unbranded.** No platform or operator branding on any surface. "ConductorUC" is a codebase name only. Resellers are the sole brand holders, with neutral fallback. | **Accepted** | S0 | [02 §5](architecture/02-tenancy-and-branding.md#5-branding) |
| D-003 | Fastify + TypeBox + Kysely + pino + OTel for services | **Accepted** (2026-09-12) | S0 | [09](architecture/09-engineering-conventions.md) |
| D-004 | NATS JetStream event bus with transactional outbox. Redis is kept out of durable messaging because the SAD makes Redis ephemeral. | **Accepted** (2026-09-12) | S0 | [05 §5](architecture/05-data-architecture.md#5-events) |
| D-005 | One MariaDB cluster with a schema per service. Tenant isolation through a mandatory `scoped(ctx)` data-access layer. | **Accepted** (2026-09-12) | S0 | [05 §1–2](architecture/05-data-architecture.md) |
| D-006 | FreeSWITCH gets all directory, dialplan, and module config from `telephony-config` via `mod_xml_curl`. Nodes hold no tenant config. | Proposed | S1 | [03 §3](architecture/03-signaling-and-media.md#3-freeswitch) |
| D-007 | OpenSIPs is registrar, auth point, and trunk edge (`uac_registrant`, `uac_auth`, `drouting`). FS has no gateways. | Proposed | S1 | [03 §1](architecture/03-signaling-and-media.md#1-division-of-responsibility) |
| D-008 | Call flows compile to an immutable, versioned JSON IR executed by a Lua runner on FS. The alternative (generating XML dialplan or `ivr.conf` menus) is rejected because it handles branching, loops, and versioning poorly. | Proposed | S2 | [03 §4](architecture/03-signaling-and-media.md#4-call-flows-ivr--auto-attendant) |
| D-009 | Voicemail audio, greetings, and prompts live centrally in S3. Voicemail runs as a Lua app backed by `voicemail-service`, not stock `mod_voicemail` storage. This is required for non-pinned nodes. | Proposed | S2 | [03 §5](architecture/03-signaling-and-media.md#5-stateless-node-rules-for-features) |
| D-010 | Queues, parking lots, and conference rooms are pinned to one node at a time through Redis affinity leases | Proposed | S2 (abstraction) / S4 (multi-node) | [04 §3.3](architecture/04-high-availability.md#33-resource-affinity-leases) |
| D-011 | Recordings go to a transient node spool, then an uploader pushes them to S3 and deletes the local copy. No durable node storage. | Proposed | S5 | [03 §6](architecture/03-signaling-and-media.md#6-recording-pipeline-summary) |
| D-012 | Short-lived EdDSA JWT access tokens + rotating refresh cookies. MFA required for master and reseller users. | Proposed | S1 | [07 §2](architecture/07-security-and-permissions.md#2-tokens) |
| D-013 | **Reseller access to billing data.** See conflict C-1. | **Accepted** (2026-09-15, issue #95) | S2 | below |
| D-014 | Five services added to the SAD §7 list: api-gateway, pbx-config-service, telephony-config, call-control, notification-service | Proposed | S1 | [06](architecture/06-services.md) |
| D-015 | Voicemail storage core is pulled forward from Phase 5 to Stage 2 (email and transcription stay in Stage 5) | Proposed | S2 | [plan](plan/implementation-plan.md) |

**Stage 0 sign-off (2026-09-12).** D-001, D-003, D-004, and D-005 were accepted together. Three of
them were already implemented and merged when they were signed off — D-001 in S0-01, D-003 and
D-005 across S0-02 and S0-03 — so the sign-off records what the code already does. D-004 was
accepted ahead of S0-04, which is the first task to build on it. Every other *Proposed* decision
still needs sign-off before the stage in *Needed by*.

## 2. Open questions

From SAD §12, with recommendations:

| ID | Question | Recommendation | Needed by |
|---|---|---|---|
| O-1 | Container orchestrator | **Hybrid.** Run FreeSWITCH and OpenSIPs on dedicated hosts or VMs as containers with host networking. Large RTP UDP port ranges and real IP visibility are awkward under Kubernetes CNI networking. Run stateless services on Kubernetes, or on Nomad if the team wants one tool for both. Develop on Docker Compose until then. Keep services 12-factor so the decision stays cheap. | S4 |
| O-2 | CDR / billing API schema | A v1 draft is in [06 §cdr-service](architecture/06-services.md#cdr-service). Freeze it in S2-11 after review against one target billing system's import format. | S2 |
| O-3 | Speech-to-text provider | Keep it behind a `TranscriptionProvider` interface and evaluate on accuracy for telephony audio (8 kHz), price, data-residency options, and data-retention terms. The first adapter is picked in S5-06. | S5 |
| O-4 | XMPP server | **ejabberd.** It has mature clustering, many virtual hosts (one per tenant), MUC, MAM with SQL storage (MariaDB), external/HTTP auth, and an admin HTTP API for provisioning. Prosody is the lighter alternative if clustering isn't needed. | S6 |

New questions raised during breakdown:

| ID | Question | Recommendation | Needed by |
|---|---|---|---|
| O-5 | Container image registry & release versioning | Semver per service. Images tagged `{service}:{version}` and `:{git-sha}`. | S0 |
| O-6 | Open-core license for platform code | Needs an owner decision. Note the upstream licenses: FreeSWITCH is MPL 1.1 and OpenSIPs is GPLv2. The platform services are separate programs that talk over network protocols, but modified upstream code or configs shipped in images must follow their licenses. Evaluate AGPLv3 (protects against closed SaaS forks) vs. Apache-2.0/MPL-2.0 (friendlier to integrators). | Before first public release |
| O-7 | Media anchoring (RTPengine) at the edge | Deferred. Without it, FS node media IPs appear in SDP, so topology hiding covers signaling only. Decide in S4 based on NAT and topology-hiding needs. | S4 |
| O-8 | Which carriers get SMS adapters first | Owner to name the carriers most common among target resellers | S7 |
| O-9 | Bucket-per-tenant vs. prefix-per-tenant | Implement both in `@cuc/storage`, with per-tenant buckets as the default per the SAD. Confirm the chosen S3 provider's bucket limits before production. | S5 |
| O-10 | Per-tenant logo under a reseller brand | Not in v1. Resellers brand; tenants inherit. | Post-v1 |
| O-11 | Where the operator's own direct customers live | A normal "house" reseller. Master still provisions resellers only. | S1 |
| O-12 | Recording consent and legal notices | Provide a per-policy consent announcement. Legal defaults are the tenant's responsibility and are documented as such. | S5 |
| O-13 | Recordings in flight when a node dies | Accept loss in v1, or add a replicated spool (for example, a small per-node volume replicated via the uploader). The recommendation is to accept loss and alert. | S5 |
| O-14 | In-browser listen/whisper/barge | Out of scope (no softphone). The supervisor's own SIP device is used. Revisit if WebRTC is ever added. | S5 |
| O-15 | Bridge SIP "on a call" state into XMPP presence | Nice to have. Optional adapter in S6. | S6 |
| O-16 | Moving a tenant between resellers | Master-only, audited, post-v1 | Post-v1 |

## 3. Conflicts & gaps found in the SAD

| ID | Issue | Detail | Proposed resolution |
|---|---|---|---|
| **C-1** | ~~Billing API vs. the reseller private-data wall~~ **Resolved** (issue #95, 2026-09-15) | SAD §6 says the CDR/billing API lets **tenants/resellers** integrate billing, but SAD §3 says resellers have **no access** to tenant CDRs. Resellers usually do the billing. | A separate **billing record** (usage data class): tenant, call ID, start, billable seconds, direction, trunk ID, destination number or prefix. Leave out recordings, legs, caller names, internal extension detail, and SIP metadata. Resellers get this through a `billing.read` API key. Full CDRs remain tenant-private, never readable by a reseller (H1 stands). Owner's answers (issue #95): (1) resellers see the **full destination number**, not a truncated prefix — rating needs it; (2) reseller billing access is **on by default** for every tenant, not an opt-in — "the master never bills"; (3) the **master** sees more than any reseller's billing view: full CDR-level detail (not just the billing record) for every tenant, broken down by call type and extension count. |
| **G-1** | Emergency calling not addressed | A multi-line telephone system in the US is subject to Kari's Law (direct 911 dialing without a prefix, plus on-site notification) and RAY BAUM'S Act (dispatchable location). Other countries have their own rules. With BYO trunks, location delivery depends on each carrier's E911 service. | Minimum in core, in S2: direct dial without a prefix, a priority emergency route per tenant, an emergency location per extension, a notification hook (email/SMS/console) on every emergency call, and the configured location passed to the carrier in the format that carrier supports. **Resolved** (issue #96): (1) countries in scope for v1 are the **USA and Canada**; (2) a dispatchable location **is required at provisioning time** — an extension cannot be created without one (locations are their own resource, shareable across extensions — a future "Site" grouping is a natural extension of this, not built now); (3) the **reseller** carries the compliance obligation, not the tenant. **S2-06** implements this. |
| **G-2** | Voicemail on stateless nodes | Stock `mod_voicemail` stores on node-local disk, which breaks "any node serves any tenant". | D-009 (central storage), pulled into S2 (D-015). |
| **G-3** | Queues, parking, and conferences are per-node in FreeSWITCH | Active-active with non-pinned nodes still needs a single owner per live queue, lot, or room. | D-010 affinity leases. |
| **G-4** | Trunk registration with N nodes | If every FS node registered every tenant trunk, carriers would see multiple competing registrations. | D-007 (OpenSIPs `uac_registrant`, clustered). |
| **G-5** | Prompts, greetings, and MOH storage not addressed | These are tenant uploads needed on every node. | Media assets in S3, played through `http_cache` (pbx-config-service). |
| **G-6** | Toll-fraud controls not addressed | This is the largest operational risk for a PBX platform. | [07 §6](architecture/07-security-and-permissions.md#6-abuse--fraud-controls), in S2. |
| **G-7** | Email delivery has no owner | Voicemail-to-email, fax-to-email, and password reset all need email, with branding. | notification-service (D-014). |
| **G-8** | "LCR" with no shared carrier pool | LCR here can only mean choosing among a single tenant's own trunks (priority, prefix rules, failover). | Per-tenant `drouting` groups. |
| **G-9** | SMS on BYO carriers | SMS isn't carried over the SIP trunk for most carriers. It needs each carrier's API. | Adapter model (O-8). |
| **G-10** | Permission catalog has no read-counterparts for most `*.manage` verbs | [07 §3.3](architecture/07-security-and-permissions.md#33-permission-catalog) gives `master_support`/`reseller_support` "read everything"/"read config" access, but the catalog (`@cuc/authz`'s `PERMISSION_CATALOG`) has no `tenant.read`-shaped permissions to grant — only the `*.manage` verbs exist, which also imply write. Found while building the built-in roles in S1-06; the roles bundle what the catalog actually has rather than inventing permissions unilaterally. | **Owner to decide**: add a `*.read` permission alongside each `*.manage` one (roughly doubles the catalog), or redefine the two support roles' scope to name specific `*.manage` permissions they may use read-only via a route-level convention. Needed before S1-08 (api-gateway) or any route relies on `master_support`/`reseller_support` actually working as documented. |
| **G-11** | Org hard-delete (with retention window and data export) has no owning task | [02 §2](architecture/02-tenancy-and-branding.md#2-provisioning-responsibilities) documents the full lifecycle as `active` → `suspended` → `active`, and from either state → `pending_deletion` → `deleted` (hard delete after a retention window, with a data export offered first), but no stage task in the plan builds the export job, the retention timer, or the hard-delete worker. S1-02 implements create/read/update/suspend/resume only — its "Done when" text never mentions delete, matching this gap rather than contradicting it. | **Owner to decide**: schedule a task for the `pending_deletion` → `deleted` transition (export job, retention window, hard-delete worker) — most naturally alongside S5's retention-policy work ([S5-05](plan/implementation-plan.md)), since both need the same S3-lifecycle machinery. Until then, org-service's `suspend`/`resume` are the only lifecycle transitions implemented; nothing can reach `pending_deletion` or `deleted`. |
| **G-12** | `audit_events` partition maintenance and retention have no owning task | S1-07's migration creates monthly `RANGE COLUMNS` partitions on `at` for a multi-year window generated at migration-write time, plus a `MAXVALUE` catch-all so inserts never fail — but nothing rolls the window forward (adds next year's partitions before this one runs out) or drops partitions past [07 §4](architecture/07-security-and-permissions.md#4-audit)'s configurable retention (default 1 year). Every row still lands somewhere (the catch-all), so this is an efficiency/retention gap, not a correctness one. | **Owner to decide**: schedule a periodic job (most naturally alongside S5's retention-policy work, like G-11) that adds a partition for each upcoming month and drops/archives partitions older than the configured retention. |
| **G-13** | `audit.read`'s declared data class can't represent its actual, per-row mixed sensitivity | [07 §3.3](architecture/07-security-and-permissions.md#33-permission-catalog) lists `audit.read` as `config/private` — genuinely mixed, since one org's audit trail mixes ordinary config-change rows with rows about master's audited access to that org's private data. `@cuc/http`'s route-contract guard (rule 3) requires exactly one `dataClass` per route, and `GET /v1/orgs/:orgId/audit-events` declares `config` so `reseller_admin`/`reseller_support` (which hold `audit.read`) aren't blocked outright by H1's coarse, route-level wall — real filtering happens in the query (`actor_org_id = :orgId OR target_org_id = :orgId`), not the declared class. | **Owner to decide**: whether a route's dataClass should stay a coarse, route-level property (current model) or whether mixed-sensitivity endpoints like this one need per-row class evaluation at query time — which is what `h1PrivateDataWall`'s resource-aware form already does, just not reachable from a route that serves a heterogeneous result set. Needed before this route's H1 exposure is fully correct, not just query-filtered. |
| **G-14** | API-key authentication has no issuing/verifying endpoint | [06's api-gateway section](architecture/06-services.md#api-gateway) lists API-key auth as one of api-gateway's responsibilities, and S1-08's plan row names it explicitly, but identity-service — which [06](architecture/06-services.md#identity-service) names as the owner of API keys (`/v1/orgs/{orgId}/api-keys`) — has never built that endpoint across S1-05, S1-06, or S1-07; it remains listed there as future scope. There is nothing for the gateway to verify a presented key against. | api-gateway (this task) recognizes the `ApiKey` auth scheme and returns a distinct `501 api_key_auth_not_implemented` rather than either silently accepting it or folding it into a generic `401` — so a caller can tell "not supported yet" from "wrong key." **Owner to decide**: schedule identity-service's api-keys CRUD + verification lookup (most naturally alongside whichever stage first needs machine-to-machine API access, since nothing in scope through S1 requires it yet), then wire api-gateway's `ApiKey` branch to call it, matching the JWT branch's shape. |
| **G-15** | "All writes" and "all private/secret reads" audit coverage ([07 §4](architecture/07-security-and-permissions.md#4-audit)) is not implemented for ordinary CRUD, in any service | 07 §4 states plainly that every write and every private/secret read is written to `audit_events`. In practice, across org-service (S1-02/03/04), identity-service (S1-05/06), and now pbx-config-service (S1-09), only two call sites ever call `@cuc/audit`'s publisher: identity-service's `onUnscopedAccess` sink (cross-tenant queries only) and pbx-config-service's `:reveal` action (S1-09's own task text names it explicitly, so it was built for real rather than left to match this gap). Ordinary writes — creating a reseller, suspending a tenant, updating an extension's display name — produce a domain event but no audit event. | **Owner to decide**: whether every write across every service should call `recordAuditEvent` alongside its domain event (a cross-cutting retrofit, not one task's job) — most naturally done as its own stage-spanning task once enough services exist that the pattern is stable, rather than three separate services each guessing at the right shape independently. Until then, `audit_events` reliably covers cross-tenant reads and the one action (`:reveal`) whose task text required it, and nothing else. |
| **G-16** | telephony-config's reconciliation job (06: "compares the read model with the source services") can only check itself, not the source services | S1-12 built the `org.*`/`pbx.*` event-driven projection and a real reconciliation pass, but that pass only diffs telephony-config's own local read model against what is actually projected into the `opensips` schema — it repairs a failed or hand-edited `opensips` write, not a read model that itself drifted from org-service/pbx-config-service (e.g. a missed event that never redelivered). Neither upstream service exposes a "list everything" internal endpoint (org-service has `GET /internal/v1/tenants/:id/domain`, S1-09; pbx-config-service now has `GET /internal/v1/tenants/:tenantId/extensions/:id`, S1-12 — both single-record lookups, not listings), and building one well (pagination, at minimum) is its own piece of work. | **Owner to decide**: add paginated internal listing endpoints to org-service (tenants + primary domain + status) and pbx-config-service (extensions + digest credential per tenant), then extend `services/telephony-config/src/reconcile.ts` to diff against them too. Until then, an event that is silently lost before `consumed_events` records it (a crash between JetStream ack and commit is excluded by design, but a bug in the handler that returns early is not) has no automatic repair path. |
| **G-17** | CI's shared `TEST_NATS_URL` is unsafe once more than one service tests the same event subjects with a real bus | `.github/workflows/ci.yml` sets `TEST_NATS_URL` globally so every package's `vitest run` points at the *same* JetStream server — `packages/testing/src/nats.ts`'s own doc comment already names the risk this creates ("two suites sharing one server would fight over the same stream names," which is exactly why its preferred, unset-`TEST_NATS_URL` path spins up an isolated `nats-server` per suite instead). Through S1-11, pbx-config-service's `domain.consumer.test.ts` was the only test in the repository that published real `org.domain.added` events to a real bus, so the collision this predicts never fired. S1-12 added telephony-config's own `org.*`/`pbx.*` consumers under test, and turbo running both packages' `test` tasks concurrently against the shared CI server produced the predicted symptom twice: once as an inflated `pass.handled` count on a first pull, and — after loosening that assertion — again as a stray message landing on a *second*, otherwise-expected-empty pull in a `redelivery does not recompute/re-project twice` test. | Every affected `pass.handled`/`first.handled` assertion was loosened from `toBe(1)` to `toBeGreaterThanOrEqual(1)` (the tenant-specific outcome each test asserts afterward is unaffected by a stray unrelated event). The `redelivery does not [re]project/recompute twice` tests in both services no longer assert `toBe(0)` on a second pull at all — instead they name the exact event id published and check `consumed_events` has exactly one row for it, which is what the dedupe mechanism (an insert on `id`, its primary key) actually guarantees, and is immune to any other concurrent traffic on the shared stream. **Owner to decide**: whether to also stop setting `TEST_NATS_URL` in CI (each package isolates itself via its own spawned `nats-server`, at the cost of N spawns) or namespace test subjects/streams per CI job — the per-test fixes above remove the symptom but not the shared-server cause, which could still surface a novel flake somewhere neither fix anticipated. **S1-14 hit exactly that**: `org.consumer.test.ts`'s "suspending a tenant" and "ignores a reseller base-domain event" tests, and `pbx.consumer.test.ts`'s "extension.deleted" test, asserted the *entire* projected domain/subscriber list was empty rather than checking their own row — a different suite's concurrently-published, correctly-scoped event landed in the same pull and broke them in CI (reproduced exactly by injecting that same event locally). Fixed the same way as the assertions above: check the specific row each test actually cares about, not global emptiness. The underlying shared-server cause was still open, and kept producing failures: a test in one package purges the shared `ORG` (or `PBX`, `AUDIT`) stream in its cleanup while another package's test has published to it, so the event is gone before it is pulled and the test fails after its 15 s of retries (seen on `pbx-config-service`'s `domain.consumer.test.ts` in CI, a different test each time). **Resolved:** CI no longer sets `TEST_NATS_URL`. It installs a pinned, checksum-verified `nats-server` binary, and `startTestNats` starts one isolated server per suite, as its own doc comment always preferred. Verified locally: the seven packages that use a bus ran at the same time and passed. |
| **G-18** | Nothing seeds OpenSIPs' `dispatcher` table with the FS node pool, and `opensips.cfg.template`'s own comment claiming otherwise was aspirational, not implemented | 03 §1's "load balancing across FS nodes" and S1-11's `opensips.cfg.template` both assume a populated `dispatcher` table (`ds_select_dst(1, 4)`, the path a registered phone's outbound call takes to reach FS), and the file's own comment says "The FS set itself is provisioned as environment config" — but no script, migration, or compose seed step has ever written a row there. Through S1-12 this went unnoticed because no test exercised a live call end to end. S1-13's own real-FreeSWITCH verification (this task) surfaced it while confirming ext→ext dialplan routing: a registered phone's call never reaches FS at all today, in any deployment of this compose stack — `ds_select_dst` simply fails and the caller gets a 503. (Separately, S1-13 *did* add the other missing half of ext→ext — `route{}`'s "request from FS: lookup(location) and relay" branch, 03 §2.1 — since that one is squarely what this task's own dialplan response depends on to mean anything on a live call; this gap is the one piece deliberately left for its natural owner instead of expanded into here.) | **Owner to decide**: where FS-pool seeding belongs — most naturally `infra/compose`'s own dev/test bootstrapping (alongside `seed.sh`) for this stage, since production dispatcher management is presumably a future task's concern (dispatcher rows would eventually come from a real node-registry/health-check system, not a static seed). Needed before S1-14's live SIPp scenarios can complete a real call through the dispatcher path — S1-13's own "Done when" (the xml_curl endpoints, their XML shape, and the p99 benchmark) does not depend on it and was verified without it, by sending SIP directly to FS and directly to OpenSIPs rather than through the full trunk. **Fixed in S1-14**: `telephony/opensips/seed-dispatcher.py`, run idempotently from the container entrypoint. |
| **G-19** | FreeSWITCH could not originate *any* outbound SIP call, on any profile, since S1-10 | `sip_profiles/internal.xml`'s `username` param reused `vars.xml`'s `sip_identity` var — the neutral "SIP Media Server" string CLAUDE.md rule 1 requires for the `User-Agent`/`Server` headers — for the SDP `o=`/`s=` line's username token too. RFC 4566's `o=` grammar requires that field to be a single token with no embedded whitespace; sofia-sip's own outbound message construction (`nua_client_init_request0()` → `sip_object(msg)`) failed internally on the space, before any packet was ever sent, reported only as an opaque "900 Internal error"/`NORMAL_UNSPECIFIED`. S1-10's own "Done when" never exercised an outbound call (only inbound registration/dialplan), so this went uncaught for four stages until S1-14 first tried one — every deployment of this compose stack has been unable to originate a call, ever, since S1-10 shipped. | **Fixed in S1-14**: a second, space-free var (`sip_sdp_identity`, `vars.xml`) used for the SDP username specifically, distinct from `sip_identity` (which keeps its neutral, space-containing value for the header case). Confirmed via `fs_cli originate` producing real network I/O (a timeout to an unreachable IP, a real SIP rejection from a reachable one) instead of an instant internal error. |
| **G-20** | OpenSIPs' in-dialog request handling (`has_totag()` branch) used `loose_route()`, but `topology_hiding()` never sets a Record-Route at all | S1-11's `route{}` matched the textbook loose-routing pattern (`loose_route()` failing → 404), but `topology_hiding()` (loaded and called earlier in the same file, S1-11) hides topology purely by rewriting the Contact header into an opaque, encoded form — confirmed against the module's own docs — and never adds a Record-Route for `loose_route()` to find. Every in-dialog request (BYE, re-INVITE) on every call this platform has ever proxied has been rejected with a 404, silently, since S1-11 — nothing before S1-14 tried a full call including hangup, so it went uncaught for three stages. | **Fixed in S1-14**: the `has_totag()` branch now calls `topology_hiding_match()`, the module's own purpose-built replacement, which decodes the hidden Contact/R-URI pair directly. The SIPp test scenarios' own ACK/BYE also had to start targeting that hidden Contact URI as their Request-URI (`tests/sip/scenarios/uac_call.xml`) — per RFC 3261, that's what a UAC should always do for an in-dialog request regardless of topology hiding, so this half was a test-harness bug, not a second platform bug. |
| **G-21** | `tools/brand-leak`'s configured surfaces don't match the file extensions telephony's config actually uses | Its own run output warns `telephony/**/*.cfg`, `*.conf`, `*.lua`, `*.tpl` "matched no files" — but the real files are `opensips.cfg.template` (`.template`, not `.cfg`/`.tpl`) and FreeSWITCH's `vars.xml`/`sip_profiles/*.xml` (not listed under any `telephony/**` glob at all). Rule 1 explicitly names "SIP headers, SDP" as a covered surface, and this task's own G-19 fix lived in exactly these files — they were never actually scanned by the automated tool that's supposed to catch this class of mistake; every check this session was a manual, human read of the diff. | **Owner to decide**: correct the glob list in `tools/brand-leak`'s config to the real extensions (`**/*.template`, and an explicit `telephony/freeswitch/conf/**/*.xml` or similar) and set `failOnEmptyInclude` once they match real files, per the tool's own warning message. |
| **G-22** | `trunks.caller_id_policy`'s shape is not specified anywhere | [05 §3.4](architecture/05-data-architecture.md#34-trunk-service) lists the column but, like every table in that section, gives no shape beyond "key columns" — and S2-04 ("Caller ID comes from the extension, DID, or trunk policy") is the task that actually needs to define the precedence rule this column feeds into, not S2-01. | S2-01 (this task) shipped a minimal `{ name: string \| null, number: string \| null }` — the trunk's own caller-ID fallback, requiring at least one of the two — so the column exists and is settable per its "Done when" text, without guessing at precedence rules that are S2-04's decision. **Owner to confirm** this shape when S2-04 defines the full extension/DID/trunk precedence; a later migration can extend it (e.g. a `privacy` flag) compatibly. |
| **G-23** | `drouting`'s `dr_rules` and `dr_groups` tables are not projected by S2-02, and `route{}` has no outbound-to-PSTN branch at all | S2-02's plan line names `dr_gateways`/`dr_rules`/`dr_groups` together, but `dr_rules`' content (prefix, priority, gateway order) comes from a tenant's *outbound routes* — a `trunk-service` entity S2-01 explicitly left out of scope (05 §3.4 lists `outbound_routes` under trunk-service, but only S2-04 builds it) — and `dr_groups`' own keying (which `(username, domain)` pair `do_routing()`'s group-selection step matches against) is inseparable from the outbound-call `route{}` branch that would actually call `do_routing()`, which also does not exist yet (`opensips.cfg.template`'s `route{}` today handles inbound-from-trunk, from-FS-to-known-location, and phone-to-elsewhere-via-dispatcher — never "FS sending an outbound-to-PSTN call"). Building either now risks guessing wrong at a convention only S2-04 can actually exercise end to end. | S2-02 (this task) projects `registrant` (register-mode trunks) and `address` (ip-mode trunks' inbound identification) fully, and `dr_gateways` (one row per trunk, independent of routing — `gwid`/`address` need no outbound-route data to be correct), since none of those three carry the same dependency. **Owner to confirm** the `dr_groups` keying convention and add the `route{}` outbound branch (`do_routing()` + `uac_auth()` + gateway failover, per [03 §2.1](architecture/03-signaling-and-media.md#21-routing-logic)) as part of S2-04, once real outbound routes exist to populate `dr_rules` from. **Fixed in S2-04**: `dr_rules` is projected (`opensips-projection.repo.ts`'s `upsertDrRule`); `dr_groups` is deliberately still not (see G-28's own resolution for why, and what replaces it). |
| **G-24** | A `registrant` row with no explicit `expiry` leaves a trunk stuck unregistered indefinitely after any single transient failure | `README.uac_registrant.gz` (confirmed against a real OpenSIPs 3.6.8 instance): "after any kind of failure (timeout, credentials, internal error), the registration is re-taken after `expires` seconds" — not `timer_interval`. `registrant.expiry` is `NULL`-able in the vendored schema, and a naive projection (write whatever `NULL` means "no opinion" as) resolves to some enormous internal default (observed live: a retry timestamp decades in the future) — so one dropped packet or a moment of DNS unavailability at REGISTER time would leave a trunk permanently `not_registered`/`failed`, never retried by the module's own timer, with no error surfaced anywhere a tenant or operator would see it. Found writing S2-02's own SIPp acceptance test: a real container's brief DNS-propagation delay reproduced exactly this, and the trunk never recovered even after the container became resolvable. | **Fixed in S2-02**: `services/telephony-config/src/repo/opensips-projection.repo.ts`'s `upsertRegistrant` always writes a real `expiry` (`REGISTRANT_EXPIRY_SECONDS`, 3600s) instead of `NULL`. Verified live: with the fix, a failed registration attempt (the same DNS-timing reproduction) retried successfully once the transient condition cleared, within the fast dev-only `OPENSIPS_REGISTRANT_TIMER_INTERVAL` window. |
| **G-25** | A DID's `destination_type` can be `ring_group`/`flow`/`queue`/`conference`/`voicemail`, but only `extension` resolves to a real call | 05 §3.3 lists all six destination types on the `dids` table itself, and S2-03's own line ("DID CRUD ... with destinations: extension, ring group, flow, queue, conference, voicemail") names every one — but only `extension` has an owning subsystem today (pbx-config-service's own `extensions` table); ring groups (S2-08), queues (S2-13), conference rooms (S2-15), flows (S2-09/10), and voicemail (S2-16) do not exist yet. | S2-03 (this task) accepts and stores every destination type on create/update (no referential check for the five without an owning table yet — `pbx-config-service/src/repo/did.repo.ts`'s `assertReferencesExist`), but `telephony-config`'s `/fs/dialplan` from-trunk lookup only ever resolves `extension` — every other type is an honest dialplan miss (a real `NOT_FOUND_DOCUMENT`, logged, not a guess at behavior). **Owner to confirm**: each later stage (S2-08/09/10/13/15/16) is what actually teaches `/fs/dialplan` to resolve its own destination type once it exists. |
| **G-26** | trunk-service's `addIp`/`removeIp` never emitted a domain event, so an ip-mode/both trunk's inbound-identification whitelist was silently never (re)projected after the trunk's own creation | S2-01 built `POST/DELETE .../trunks/:id/ips` as plain CRUD against `trunk_ips` with no `enqueueEvent` call at all — confirmed live, writing S2-03's own SIPp acceptance test: a real ip-mode trunk's `address` row in the `opensips` schema stayed empty forever after `addIp`, and every inbound INVITE from that trunk's own whitelisted source silently fell through to the registered-phone/403 branch (`route{}`'s `check_source_address` never matched anything). `create()`'s own body has no `ips` field at all (`ips` is this dedicated sub-resource, by design) — so no auth-mode/`ip` trunk's inbound identification has ever actually worked in any deployment of this compose stack, since S2-01 shipped. Undiscovered until now because S2-01/S2-02's own tests never exercised an ip-mode trunk against a real running OpenSIPs; S2-02's own acceptance test used `authMode: 'register'`. | **Fixed in S2-03**: `services/trunk-service/src/repo/trunk.repo.ts`'s `addIp`/`removeIp` now wrap their write in a transaction and enqueue `trunk.trunk.updated` (`{trunkId}`), the same event `update()` already emits — `projection.ts`'s existing `projectTrunk` re-fetches the trunk's full current state (including `ips`) on any `.updated`, so no projection-side change was needed, only the missing publish. Verified live: an `addIp` call now produces a real `address` row and an `address_reload` MI call within the same window every other trunk event does. |
| **G-27** | `buildDialplanDocument`'s `destination_number` dialplan condition passes the dialed number straight into a PCRE `^...$` expression, uncensored | `xml.ts`'s `<condition field="destination_number" expression="^${destinationNumber}$">` was only ever exercised with plain-digit extension numbers (S1-13), which happen to contain no regex metacharacters — a DID's E.164 form always leads with a literal `+`, and FreeSWITCH's regex compiler rejects `^+...$` outright ("COMPILE ERROR: nothing to repeat": `+` has no preceding atom to quantify). Found live writing S2-03's own SIPp acceptance test: the call reached FS's dialplan, got a real (not not-found) document back, and then silently got no route at all — `switch_core_state_machine.c`'s own "No Route, Aborting" — a confusing symptom to trace back to a regex compile failure one layer down, since nothing in telephony-config's own logs showed anything wrong. | **Fixed in S2-03**: `services/telephony-config/src/xml.ts` adds `escapeRegex` (escapes PCRE metacharacters, not XML ones — a second, distinct escaping pass from `escapeXml`) and applies it to `destinationNumber` before it goes into the `expression` attribute. Verified live: a `+`-prefixed DID's dialplan condition now compiles and matches correctly. |
| **G-28** | `do_routing()`'s `groupID` parameter is compile-time-only, contradicting `README.drouting`'s own documented type | The README declares `groupID` as `(int, optional)` with no caveat, and passing a runtime pvar (`do_routing($avp(dr_group), ...)`, this task's first design) is the natural reading of "int" in a script language that otherwise accepts pvars for int params freely (`ds_select_dst`, etc., elsewhere in this same file). Confirmed live: OpenSIPs refuses to even *parse* the config with a pvar there — `ERROR:core:get_cmd_fixups: Variable in param [1] is not an integer`, a config-load-time fixup failure, not a runtime one — so no per-call tenant group id can ever reach this parameter, by a header-sourced AVP or any other means. This invalidated this task's first working design (an explicit `X-Dr-Group-Id` header FS's own outbound dialplan document set, read back via `$hdr()`/`$avp()`) after it was already built and unit-tested — caught only when live-verifying against a real OpenSIPs process, exactly the class of mistake `docs/decisions.md`'s own existing G-19/G-20/G-24 entries warn a doc-only design can hide. | **Fixed in S2-04**: tenant isolation moved out of `do_routing()`'s group mechanism entirely and into `$rU` itself — a fixed-width, zero-padded tag (the tenant's own `tenant_dr_groups.dr_group_id`, `opensips-projection.repo.ts`'s `DR_TAG_WIDTH`) is prepended to the dialed number by FS's own outbound dialplan document (`xml.ts`'s `buildOutboundDialplanDocument`) before the re-INVITE ever reaches OpenSIPs, and `dr_rules.prefix` carries the same tag, so the prefix trie can only ever match that one tenant's own rules (the matched gateway's `strip` is widened by `DR_TAG_WIDTH` to remove the tag again before dialing out). `do_routing()`'s own group param is called omitted, backed by a single shared `default_group` (modparam) constant — safe, since the tag already does the real isolation. Verified live: two tenants' own outbound routes projected side by side, each tenant's own call only ever matches its own rule. |
| **G-29** | `drouting`'s `dr_rules.prefix` rejects a leading `+`, contradicting this task's own E.164-normalized pattern design | `outbound-route.ts`'s `PATTERN` (`/^(\+\d*)?$/`) and `e164.ts`'s `normalizeToE164` both assume a leading `+` is the natural, E.164-native shape for an outbound-route pattern and a normalized dialed number — reasonable given every other E.164 value on this platform (DIDs, caller ID) keeps it. `README.drouting` documents `dr_rules.prefix` as "string with digits only," confirmed live: projecting a `+1`-pattern route crashed `dr_reload` outright (`ERROR:drouting:add_prefix: + is not valid char in the prefix`, `dr_load_routing_info: failed to add rule ... skipping` — the rule silently vanishes from the live routing table, not merely a cosmetic warning) — every outbound call on a `+`-prefixed pattern would have gotten a 404 in production, undiscovered until a live `dr_reload`. | **Fixed in S2-04**: the domain layer keeps `+`-prefixed patterns and E.164 numbers throughout (`outbound-route.ts`, `e164.ts`, `fs.routes.ts`'s own coarse route pre-check) — the leading `+` is stripped only at the two OpenSIPs-projection boundaries that actually need digit-only values: `dr_rules.prefix` (`projectOutboundRoute`) and the dialed number FS bridges to (`buildOutboundDialplanDocument`, which also never includes a `+` in a `sofia/...@...` dial-string user part to begin with). Verified live: a `+1`-pattern route now loads into `dr_reload` with zero discards. |
| **G-30** | `tests/sip/src/run-scenario.ts`'s `uasReceivedCall` has never actually detected a received call — every existing caller only asserted `false`, and it always returns `false` | Matched SIPp's "Statistics Screen" (`Incoming call created \| periodic \| cumulative`), which a `startUas`/`startBackgroundUas` container — by design, never exits and is never sent a stats-reset signal — never prints; it only ever prints the *different*, periodic "Scenario Screen" instead (confirmed live, grepping a real background UAS's full `docker logs` after a real, successfully-handled call: zero occurrences of "Incoming call created," ever). Since S1-14, every caller of this function (`scenarios.test.ts`'s two tenant-isolation tests) only ever asserted the negative case (`toBe(false)`, "this UAS must never be called") — which this bug made trivially, vacuously true regardless of whether isolation actually held, since the function could never return anything else. Found building S2-04's own failover test, the first caller to assert `toBe(true)` — the assertion failed even though two independent manual reproductions (a direct `fs_cli originate` and a real SIPp caller run outside the test harness) proved the underlying routing correct, which is what narrowed this down to the *check* being broken, not the platform. | **Fixed in S2-04**: matches "Scenario Screen" 's own `Peak was N calls` line instead — present in every SIPp log regardless of exit/reset state, and monotonic for the container's lifetime. **Owner to confirm**: `scenarios.test.ts`'s two existing isolation tests still pass with the fixed detector (they do — re-run live, S2-04), but were never actually exercising the codepath they claimed to since S1-14; worth a skeptical re-read if tenant isolation is ever suspected to have regressed, since this is the only test that would have caught it. |
| **G-31** | S2-05's outbound calls-per-second limit is a single shared platform-wide constant (10/s), not `orgs.limits.maxCallsPerSecond` | `rl_check(name, limit, algorithm)`'s own `limit` param is documented as plain `(int)`, the identical wording that turned out to mean "compile-time constant, not a runtime pvar" for `do_routing()`'s `groupID` (G-28) — burned once already this task, this was deliberately *not* re-risked without live confirmation either way. The one place `route{}` could read a per-tenant value from at all is `domain.attrs` (the same column `$var(tenant_id)` already comes from), which is already load-bearing for `X-Tenant-Id` on every ext→ext and outbound call since S1-13/S2-04 — changing its content format to pack a second value in would touch that already-proven plumbing for an unconfirmed payoff. | Every tenant's outbound CPS is capped at the same literal `10` (`opensips.cfg.template`'s `rl_check($var(tenant_id), 10, "TAILDROP")`) — real per-tenant isolation (the pipe *name* is `$var(tenant_id)`, confirmed safe to pass dynamically: `rl_check`'s own README sample already does this for `$rU`), just not per-tenant *configurable* yet. **Owner to decide**: either live-verify `rl_check`'s `limit` param against a real OpenSIPs process the way G-28 was, or extend `domain.attrs`'s content format (and every reader of `$var(tenant_id)` downstream) deliberately, as its own scoped change. |
| **G-32** | `reconcile.ts` has no repair pass for `dr_rules` at all — not for S2-04's `outbound_routes` and not for S2-06's `emergency_routes` | `projectOutboundRoute`'s own comment (`services/telephony-config/src/projection.ts`) already claimed this as a "docs/decisions.md gap" when S2-04 shipped, but no numbered entry was actually added then — this is that missing entry, now covering S2-06's identical gap too rather than adding a near-duplicate second one. `reconcileOnce()` (`reconcile.ts`) diffs `domains`/`subscribers`/`registrants`/`addresses`, and per-trunk `dr_gateways` (G-23's own resolution), but never touches `dr_rules` or the per-route synthesized gateways `upsertOutboundGateway`/`projectEmergencyRoute` write — a `dr_rules` row (or synthesized gateway) lost to a missed/failed event, or hand-edited out of the `opensips` schema, has no automatic repair, for either route type. Undiscovered as a live incident so far only because nothing has yet exercised that failure mode end to end. | **Owner to decide**: extend `reconcile.ts` to list every tenant's current `outbound_routes`/`emergency_routes` from the local read model (already mirrored locally — no new upstream listing endpoint needed, unlike G-16) and diff their expected `dr_rules`/synthesized-gateway rows against what `opensipsProjection` actually has, the same "desired state vs. projected state" shape `reconcileOnce()` already uses for domains/subscribers. |
| **G-33** | `X-Emergency-Location`'s header format is a generic default, not any specific carrier's real E911 delivery format | G-1/issue #96 is explicit that "location delivery depends on each carrier's E911 service," and S2-06 had no specific BYO carrier to integrate against. `xml.ts`'s `buildEmergencyDialplanDocument` sends a single-line, human-readable civic address (`addressLine1 / addressLine2 / city / state / postalCode / country`) as a custom `X-Emergency-Location` SIP header — not a PIDF-LO body, not a Geolocation header with a location-reference URI pointing at a LIS, and not any particular carrier's own documented E911 convention. A real carrier integration may require one of those instead, or in addition. | **Owner to decide**: once a specific BYO carrier's E911 delivery requirement is known, extend or replace this header with whatever that carrier's own service actually expects — `location: EmergencyLocationDetail \| null` is already threaded through `handleEmergencyDial`/`buildEmergencyDialplanDocument` (`services/telephony-config/src/routes/fs.routes.ts`, `src/xml.ts`) as a single civic-address value, so a format change is localized to those two functions, not a data-model change. |
| **G-34** | `tests/sip`'s own Docker container orchestration (`run-scenario.ts`) has timing margins too tight for reliable CI, independent of any one task's test | S2-06's own CI run (PR #128) hit this directly: three consecutive `SIP smoke` attempts each failed on a `startUas`/`startBackgroundUas` container not printing "Sipp Server Mode" in time, a `waitForContainerExit` container not exiting in time, or a `startDelayedCaller` `docker inspect` IP lookup not resolving in time — but never the *same* file twice, and never anything that implicated application logic: run 1 hit `scenarios.test.ts` (S1-14, untouched by this PR); run 2 hit `scenarios.test.ts` differently and separately `toll_fraud.test.ts` (S2-05); run 3 hit `trunk_did_routing.test.ts` (S2-03) *and* this PR's own new `emergency_calling.test.ts`, via the exact same shared `startDelayedCaller` helper every other file already used successfully many times before. That spread — four different files, two recurring failure signatures, never a logic-level symptom — points at CI-runner-side resource pressure (Docker daemon/container-start latency under load) exceeding this harness's fixed timeouts, not a defect in whichever test happened to be running when the deadline hit. A very similar single-file instance was already fixed once (the S2-05 "retry startDelayedCaller's IP lookup" fixup, itself only a 10-attempt/~2s retry) and evidently wasn't a wide enough margin for every caller under heavier load. | **Fixed in S2-06**: widened every timeout `run-scenario.ts` itself owns — `waitForLog`'s "Sipp Server Mode" wait and `waitForContainerExit`'s exit wait (10s/20s/30s → 25s/40s, named `CONTAINER_LOG_TIMEOUT_MS`/`CONTAINER_EXIT_TIMEOUT_MS`), and `startDelayedCaller`'s IP-lookup retry (10 attempts → 25, `CONTAINER_IP_LOOKUP_ATTEMPTS`) — plus `tests/sip/vitest.config.ts`'s own `testTimeout`/`hookTimeout` (30s → 60s, so a test with no per-test override still has headroom past whichever of the above it waits on). A mechanical, conservative widening only (never masks a genuine hang — a container that truly never starts or exits still eventually fails, just later), not a root-cause fix for whatever makes the CI runner slow in the first place. **Owner to confirm** this margin holds; if it recurs even at these widths, the runner's own resource allocation (not this file's timeouts) is probably the next thing to look at. |
| **G-35** | `mod_http_cache` (S2-07) has no `http_cache.conf.xml` of its own — this task deliberately did not add one | The module is loaded (`telephony/freeswitch/conf/autoload_configs/modules.conf.xml`, since S1-10) but has never had a settings file, and this task could not bring up a real FreeSWITCH process to confirm the module's own modparam names (`location`, `max-size`, and similar) live — this environment has no way to verify FS module behavior directly, and guessing at parameter names risks a config that *looks* complete but is silently wrong (a worse state than no file at all, which at least visibly relies on the module's own built-in defaults). | Currently relying entirely on `mod_http_cache`'s own built-in defaults (cache directory, size caps) — untouched, so no regression from this task's own change, but also unconfirmed as adequate for real prompt/MOH file sizes and volumes. **Owner to decide**: once a real FreeSWITCH process is available to check against (this repo's own `sip-smoke` CI job, or a developer's own `make up`), confirm the default cache location has enough disk headroom in the container image and add explicit settings if not. |
| **G-36** | The exact `fs_cli originate` dial-string/app syntax S2-07's own SIPp acceptance test (`tests/sip/test/media_playback.test.ts`) uses to exercise `http_cache://` playback directly (`sofia/external/900@<uas-container>:<port> &playback(http_cache://...)`) has not been confirmed against a real FreeSWITCH process | Same root cause as G-35 — no live FS process was reachable while building this task. The syntax is this task's own best-effort reasoning from FreeSWITCH's own documented `originate`/dial-string conventions (and matches the shape G-19's own already-verified `fs_cli originate` calls use elsewhere in this repo), not something copied from a working, previously-verified call site the way most of this session's other FS-facing code has been. | This is exactly what `sip-smoke` CI is for — a real failure here is the useful, expected way to find out the syntax is wrong, the same as G-19/G-20/G-24 were all found. **Owner to confirm**: once this test has run against a real compose stack, either it passes (closing this gap) or its own failure output is the next debugging input, the same live-verify-first discipline every other FS-facing decision in this file already followed. |
| **G-37** | org-service's own `POST /v1/resellers/:id/brand/assets` (S1-04) never calls `provisionBucket()` before presigning an upload — a bucket that was never provisioned 404s as `NoSuchBucket` on the real upload, not at presign time | Found live while building S2-07's own media-asset upload flow, which needed the identical presign-then-real-upload pattern: a direct MinIO probe (a presigned PUT against a bucket nothing had ever provisioned) reproduced a real `404 NoSuchBucket` on the upload itself. `services/org-service/src/routes/brand.routes.ts`'s own `POST .../brand/assets` handler calls `storage.forPlatform().presignPut(...)` directly with no `provisionBucket()` call anywhere in that file — meaning every deployment of this platform has had a broken reseller-brand-asset upload flow since S1-04, for any platform bucket not already provisioned by some other means. Out of scope to fix here (a different service, a different task's own code) — `services/pbx-config-service/src/repo/media-asset.repo.ts`'s own `create()` calls `provisionBucket()` first specifically because of what this probe found, so S2-07 itself does not repeat the mistake. | **Owner to decide**: add the same `provisionBucket()` call to org-service's own brand-asset upload route (`brand.routes.ts`), the same one-line fix S2-07 applied to its own equivalent flow. |
| **G-38** | ~~S2-17's new `route{}` SUBSCRIBE block ... only up to module init~~ **The SUBSCRIBE handshake itself confirmed live in S2-20 — auth, tenant scoping, and at least one real NOTIFY; the deeper dialog-state-transition proof still not attempted** | S2-20 gave this its first live SUBSCRIBE/NOTIFY traffic (a genuinely new SIP method shape for `tests/sip`, no INVITE or REGISTER involved). Confirmed live, both first try, no bugs found here (unlike almost everything else this task touched): a same-tenant SUBSCRIBE (`Event: dialog`) gets challenged the same way REGISTER is (`www_authorize("", "subscriber")`, no prior registration needed), authenticates, gets a real `200 OK`, and receives a real NOTIFY from `presence`'s own notifier — `handle_subscribe()` and the presence/pua/pua_dialoginfo module chain genuinely work end to end for the accept path. A cross-tenant SUBSCRIBE (a tenant A extension's own credentials, targeting a presentity URI in a different tenant's domain) genuinely gets `403` — `$fd`/`$td` do hold the domains `opensips.cfg.template`'s own tenant-scoping check assumes, confirmed rather than guessed. Both `tests/sip/test/presence.test.ts`'s own tests, run clean 3/3. NARROWED, not attempted: the deeper proof issue #39's own "Done when" also describes — a presentity's own dialog state actually transitioning through early/confirmed/terminated as it makes/receives a real call, with each transition producing its own distinct NOTIFY body — needs a *third*, concurrently-running SIPp process placing a call to the presentity while the watcher's own SUBSCRIBE is active, which this task ran out of scope to build. The one NOTIFY this task's own test does receive is presence's own initial/default-state notification on subscribe, not proof of a state transition. | **Narrowed, not closed**: the SUBSCRIBE-accept and tenant-scoping risk this entry originally flagged is retired. **Owner to decide**: whether the dialog-state-transition proof (early/confirmed/terminated, each with its own NOTIFY) is worth a follow-up task's own three-SIPp-process scenario, or whether the accept-path proof here is sufficient confidence for BLF to ship. |
| **G-39** | `scenarios.test.ts`'s `sip-test-happy-uas` container (the "completes a real call end to end" test) failed `waitForContainerExit` four consecutive times on the self-hosted runner (2026-09-22), always the identical signature — a departure from G-34's own "never the same file twice" pattern | Checked rather than assumed to be the same flake class, given the repetition: reproduced the *entire* `tests/sip` suite locally, same execution order, same code, against a real compose stack built from the same commit — 13/13 passed cleanly in under 90s total, no exit-timeout anywhere. That rules out application logic and this harness's own scenario/assertion code. `docker compose logs` (the CI job's own failure-diagnostics step) never shows this container's own output since it's a standalone `docker run`, not a compose service — no further detail was available from the CI logs themselves, and this environment has no shell access to the self-hosted runner to investigate its own resource/disk state directly. | **Mitigated, not root-caused**: `CONTAINER_EXIT_TIMEOUT_MS` widened 40s→90s, `CONTAINER_LOG_TIMEOUT_MS` widened 25s→45s (`tests/sip/src/run-scenario.ts`) — the same mechanical, conservative-widening-only remediation G-34 already established, given a real, reproducible slowness specific to that one runner and that one container's shutdown, with no way to inspect the runner directly from here. **Owner to confirm** this margin holds, and if the runner's own disk/resource state is inspectable directly, check for accumulated Docker cruft (dangling images/volumes/build cache) from the many CI runs this session produced — `docker system df` on the runner itself would be the first thing worth checking. |
| **G-41** | ~~S2-16's voicemail dialplan/Lua surface ... unconfirmed~~ **`mod_curl`'s real call shape fully confirmed and fixed in S2-20 (a cross-cutting finding, not voicemail-specific); the `continue_on_fail` bridge fallback confirmed broken, unresolved; the DID-to-mailbox path works end to end** | S2-20 gave this its first live FreeSWITCH process and found `mod_curl`'s own `curl` API command is not remotely what every FS-facing Lua script in this codebase (`voicemail.lua`, `conference.lua`, `flow_runner.lua`) assumed — not a curl-CLI lookalike at all. Confirmed live, with a raw packet capture off a netcat listener, in this order: (1) `shell_exec` (the original way of base64-encoding a `Basic <token>` header) is not a registered FS API command in this image (`show api` has no such entry). (2) Even with a real base64 encoder, `-H`/`-d`/`-X`/`-T` flags do not exist — `mod_curl`'s real syntax (`fs_cli -x curl` with no args) is `curl <url> [ONE option] [method [data]]`. (3) That "one option" limit is real: `mod_curl`'s own usage string documents `append_headers <n:v>[|append_headers <n:v>]` as chainable, and it is not, in this FreeSWITCH build — the literal `\|...` text gets appended onto the first option's own value instead of starting a second one, confirmed the same way. (4) Since `Basic <token>` always has a space right after `Basic`, and this parser truncates any option value at its first space, no Basic-auth header could ever have survived it intact regardless of (1)-(3) — a fifth, independent reason the original approach could never have worked. Fixed by retiring Basic auth for every Lua-originated call in favor of a single, space-free `X-Fs-Node-Token: <token>` header (the one shape that survives `append_headers` intact) — `authorized()` in `fs.routes.ts` now accepts either that or the original Basic form (`mod_xml_curl`'s own requests, built internally by libcurl rather than by hand, were never affected by any of this and keep using Basic). Content-Type has the identical one-option limitation, so it's no longer set at all; `fs.routes.ts`'s shared `application/x-www-form-urlencoded` content-type parser was extended to sniff a JSON-shaped body (starts with `{`) and `JSON.parse` it instead of URL-decoding, since every Lua-originated POST body genuinely is JSON text `mod_curl` can only ever mislabel, never `mod_xml_curl`'s own real form-encoded fields. A sixth bug, voicemail-specific: `voicemail.lua`'s own `jsonField` required quotes around a matched value (correct for the string fields it was first used for, `messageId`/`uploadUrl`, which is why `leaveMessage` never caught it) but never matches a bare JSON boolean — `/verify-pin`'s own `{"valid":true}` has no quotes around `true` at all, so a *correct* retrieval PIN silently failed the exact same way a wrong one would, indistinguishable without a live trace. `conference.lua`'s own copy already had the fix (optional quotes) from its own earlier debugging this same task did (G-50); this one just hadn't been back-ported. Verified end to end (`tests/sip/test/voicemail.test.ts`, run clean repeatedly): a DID bound directly to a mailbox (`destinationType: 'voicemail'`) genuinely creates a message row via `voicemail.lua leave`, and the mailbox owner retrieves it via `*97` with a real, *correct* PIN sent over RFC 4733 DTMF (not just a rejected wrong one, unlike G-50's own conference PIN test) — matching FS's own unprompted BYE once the retrieval flow ends, not a scripted one. Genuinely NOT fixed: (a) the bridge-then-`continue_on_fail`-fallback path (`buildDialplanDocument`'s own voicemail param, `xml.ts`) — tried first, with a real registered UAS scripted to reject with 486 Busy Here (a real, deterministic hangup cause, not an unregistered-extension guess), and found live, with a full packet trace, that FreeSWITCH relays the B-leg's 486 straight back to the caller instead of continuing to `voicemail.lua` — neither the original cause-list form nor the catch-all `continue_on_fail=true` (combined with `hangup_after_bridge=false`, per FreeSWITCH's own docs) made any difference, identical relayed response both times. This matches a known, unresolved upstream FreeSWITCH issue closely enough to treat it as that (signalwire/freeswitch#2591, "continue_on_fail setting does not take effect", filed 2024), not a config mistake this task can fix — the DID-to-mailbox path this task's own test now uses instead has no such dependency and is a real, working alternative for direct-to-voicemail routing, but a busy/no-answer *extension* still cannot fall through to voicemail today. (b) The audio upload step (`leaveMessage`'s own PUT to a presigned URL) still uses the same wrong curl-CLI-flag syntax (`-X`/`-T`) everything else here just got fixed away from — whether `mod_curl`'s own `put` can stream a local file as the body at all (the one thing this step needs) is unconfirmed and looks unlikely given everything else found; the test only asserts a message *row* exists, not that its audio successfully uploaded. (c) The `*97` feature code and the 7=delete/9=save retrieval digits remain this task's own arbitrary, undocumented choice — untested beyond "doesn't break the call". | **Narrowed, not closed**: the `mod_curl` call-shape risk is fully retired, for every Lua script in this codebase, not just voicemail — this was the single highest-value fix in this pass. **Owner to decide**: (a) whether the no-answer/busy `continue_on_fail` fallback needs an upstream FreeSWITCH bug report or a version bump before it can ever work, given the DID-to-mailbox path already covers the "give callers a voicemail box" need without it; (b) how `leaveMessage`'s own audio upload should actually work — `mod_curl`'s `put` with an inline (not file-streamed) body read into a Lua string first is the most likely fix, if the recording is small enough to hold in memory, but that's unconfirmed. |
| **G-42** | `voicemail.mailbox.mwi_changed` (S2-16) has no consumer yet — MWI-over-OpenSIPs-presence is not wired | 06's voicemail-service section says "MWI is sent through the OpenSIPs presence `message-summary` PUBLISH," but no service in this codebase publishes OpenSIPs presence state yet (S2-17 built OpenSIPs presence/BLF for extension dialog-info, not MWI specifically — see G-38 above). `voicemail-service` enqueues `voicemail.mailbox.mwi_changed` (thin: `{mailboxId}`, 06's "consumer re-fetches current state" rule) on every mailbox unread-state change, but the event currently has no subscriber — this is intentional groundwork, not a bug, but it means "The MWI NOTIFY reaches the subscribed phone" (S2-16's own "Done when") does not yet hold end to end. | **Owner to decide**: once presence-publishing infrastructure exists (following on from S2-17, or a dedicated task), add a consumer that receives `voicemail.mailbox.mwi_changed`, calls `GET /internal/v1/tenants/:tenantId/voicemail/mailboxes/:id` for the current unread count, and issues the OpenSIPs `message-summary` PUBLISH. |
| **G-43** | S2-10's FreeSWITCH-side surface — the `http_cache://` media URL with inline Basic-auth credentials, `playAndGetDigits`' argument order, and the `bridge`/`continue_on_fail` interaction for `extension`/`ring_group` nodes — is unconfirmed against a real FreeSWITCH process; `flow_runner.lua`'s own `mod_curl` call shape (`httpGet`) is fixed but not yet live-run | Same root cause as G-35/G-36/G-41: no live FS process was reachable while building this task. `flow_runner.lua`'s own `httpGet` had the identical `mod_curl`-syntax bugs G-41 found and fixed for `conference.lua`/`voicemail.lua` (`-H` is not a real option; `mod_curl` only reliably takes one option per call; a `Basic <token>` header always has a space and gets truncated) — fixed the same way, `append_headers X-Fs-Node-Token:<token>`, matching `authorized()`'s own now-dual-scheme check in `fs.routes.ts` — but this task never built a live flow/auto-attendant SIPp scenario to actually run it, so that fix is unverified in practice, only by the same reasoning that found and fixed it elsewhere. `json.lua`, the hand-written IR parser the runner depends on, remains unit-tested for real (`tests/lua`, driven through a real Lua interpreter, including the malformed-input cases the runner branches on) but unexercised inside a real call. The credential-in-URL form (`mediaUrl`) is unrelated to any of the `mod_curl` findings above (a different module, `mod_http_cache`, which builds its request natively via libcurl rather than a hand-assembled command string) and remains exactly as unconfirmed as before. | Issue #44 (S2-20, the M2 backend SIP regression suite) is where this gets its first live proof — still true, since this task ran out of scope before building the auto-attendant scenario itself, the same live-verify-first discipline G-19/G-20/G-24/G-35/G-36/G-41 already established. **Owner to confirm**: if the two-level auto-attendant scenario fails there, check the media-playback auth first — a 401 from `/fs/media/...` would present as silence on an answered call, not as an obvious error — and separately confirm the `httpGet` fix inherited from G-41 actually works once a real call exercises it. |
| **G-44** | The `time_condition` flow node cannot actually branch on time: S2-09's IR gives it only a `timezone`, while 03 §2.3 describes it as keyed on a `scheduleId` | 03 §2.3's own example node is `{"type": "time_condition", "scheduleId": "…", "out": {"open": …, "closed": …}}`, but `@cuc/callflow-ir`'s `TimeConditionConfig` (S2-09) carries a `timezone` and nothing else, with ports named `match`/`noMatch` — and schedules have no owning subsystem until S3-08 ("Tenant screens: ... schedules"). With no hours to compare against, S2-10's runner takes the `match` port unconditionally and logs that it did. Inventing a default business-hours window (Mon-Fri 09:00-17:00 being the obvious temptation) was deliberately rejected: it would route after-hours callers down a `noMatch` branch on a rule nobody configured, which is a worse failure than not branching. `flow_runner.lua` already reads an optional `businessHours` config if one is present, so a schema addition works without touching the script. | **Owner to decide**: whether S3-08's schedules become a referenced `scheduleId` (03 §2.3's shape, needing an IR change and a resolver endpoint) or inline hours on the node's own config (no new lookup on the call path). Until then a published flow containing a `time_condition` is effectively a straight-through edge, which is worth saying out loud to anyone authoring one. |
| **G-45** | ~~telephony-config's `/fs/configuration` xml_curl binding cannot filter by lease ownership~~ **Resolved in S2-13** | Was: FS's stock `section=configuration` xml_curl POST carries no node-identity field (04 §3.3's own gap, flagged in S2-12). S2-13 resolves it directly, as this entry's own "resolve directly in S2-13" note anticipated: `xml_curl.conf.xml`'s `configuration` *and* `dialplan` bindings now carry `?nodeId=$${cuc_node_id}` on their `gateway-url`, and `fs.routes.ts`'s `/fs/configuration` handler reads it to filter `callcenter.conf` to queues leased to the requesting node. | Closed. `/fs/directory` still carries no `nodeId` param — nothing needs it there, and adding it speculatively would be scope with no use. |
| **G-46** | ~~The flow runner's "hairpin" transfer ... is still practically unreachable~~ **Now reachable in practice, and still unhandled** (S2-19) | `flow_runner.lua`'s `hairpinTransfer` sets `sip_h_X-Affinity-Node` and bridges back out through OpenSIPs exactly as 04 §3.3 describes. S2-13's `queue` node handler calls it conditionally (`GET /fs/flow/.../queue/:queueId`'s `isLocal: false` case) — a real code path — but `opensips.cfg.template`'s own `route{}` still does not parse that header, and S4-05 ("Affinity routing at OpenSIPs + multi-node lease tests") is the plan's own task for wiring OpenSIPs' dispatcher to it. This entry originally read "with exactly one FS node in the dev stack, `isLocal` can never actually be `false` yet — reachable in code, not in practice." **That is no longer true.** S2-19 gives the dispatcher two real nodes and a round-robin algorithm (`ds_select_dst(1, 4)`), with no session affinity across separate calls — so a *second*, unrelated call into a resource already pinned to node A (a queue, parking lot, or conference room another caller is already using) can genuinely round-robin to node B instead. Node B's own `handleQueueDial`/`handleParkDial`/`handleConferenceDial` then calls `acquireAffinity` with itself as `preferredNodeId`, gets back node A (the existing holder) since a live lease is not reassigned, sees `acquired.nodeId !== nodeId`, and returns `NOT_FOUND_DOCUMENT` — a real dialplan miss, not a test artifact. Without S4-05's OpenSIPs-side consumption of `X-Affinity-Node` to redirect that caller to node A instead, this is a genuine, user-facing call failure for any resource with more than one live participant across two round-robin-separated calls, not merely a coverage gap. S2-20's own SIPp scenarios for queues/parking lots/conference rooms (each of S2-13/14/15's own deferred live proof) are pinned to a single dispatcher destination for exactly this reason, rather than exercising it — see those scenarios' own setup for how. | **Confirmed by design, not a defect introduced by S2-19** — the plan always deferred this fix to S4-05 — **but no longer a theoretical one**: any manual/production two-node testing of queues, parking, or conference rooms before S4-05 lands will hit this for real. **Owner to decide**: whether S4-05 should be pulled earlier than Stage 4 given S2-19 already makes the gap observable, or whether documenting it here (and pinning S2-20's own tests around it) is sufficient until Stage 4's own schedule reaches it. |
| **G-47** | ~~`mod_callcenter`'s real wire shapes ... unconfirmed~~ **Four real bugs found and fixed in S2-20; a fifth, closely-related one found and left open** | S2-20 gave this its first live FreeSWITCH process. Four genuine, load-bearing bugs, all fixed: (1) same bug class G-50 found for `mod_conference` — `mod_callcenter` fails to *load at all* without `autoload_configs/callcenter.conf.xml` existing (`mod_callcenter.c:1512 Open of callcenter.conf failed`), even though `xml_curl.conf.xml`'s own `configuration` binding already serves real per-tenant queue data on demand; fixed with a minimal static bootstrap file (empty `<queues>`/`<agents>`/`<tiers>`). (2) `mod_local_stream` has the identical missing-config load failure, but its consequence is worse: `queue.mohUrl ?? 'local_stream://moh'`'s own fallback isn't merely silent without the module loaded, it's a completely unrecognized file format, and `mod_callcenter` retried opening it in a tight, unbounded loop (`switch_core_file.c:346`, thousands of times a second) for as long as a caller sat in queue — a hard hang. Fixed with a minimal `local_stream.conf.xml` pointing at an empty directory (confirmed live: an empty directory degrades to comfort noise, not a load error). `mod_http_cache` has the identical missing-config load failure too (found the same way, fixed the same way — `http_cache.conf.xml` — though nothing in this task's own scope depends on it loading; G-43's own `playback`/`moh-sound` questions do). (3) The agent login/logout feature codes' own dialplan action, `<action application="api" data="callcenter_config ...">`, was simply wrong — there is no dialplan application named `api` in this image's module set at all (confirmed directly: `show application` has no such entry), so *every* `*45`/`*46` attempt failed outright (`Invalid Application api`, channel hung up `DESTINATION_OUT_OF_ORDER`) before ever reaching `mod_callcenter`. Fixed by routing through a new `agent_status.lua` (the same "scripting language calls `api:executeString`" idiom `conference.lua`/`voicemail.lua` already establish). (4) Even with that fix, `callcenter_config agent set status` was *still* a silent no-op for any agent created after the node's own boot: `mod_callcenter`'s lazy, per-call reload path (triggered by `callcenter()` touching a queue it hasn't loaded yet) genuinely only loads the *queue* record, never its agents or tiers — confirmed directly, repeatedly: any number of queues loaded this way, `callcenter_config agent list`/`tier list` stay permanently empty; only the module's own one-time, boot-time config parse ever populates them. In a real deployment a node boots once and stays up indefinitely while tenants provision agents continuously, so this made agent login unreliable in practice, not just in this test's own ordering. Fixed by having `agent_status.lua` call `callcenter_config agent add`/`agent set contact` (both confirmed idempotent — re-adding an already-loaded agent returns a harmless `-ERR Agent already exist!` that `api:executeString` never inspects) before `set status`. Verified end to end (`tests/sip/test/queue.test.ts`, run clean 4/4): a real trunk call reaches a queue whose agent has genuinely logged in and shows `Available` in `mod_callcenter`'s own live state, not just "the dialplan action didn't error". Found but deliberately left open, the same bug class as (4) but on the *tier* side: a queue's own tier assignments have the identical lazy-load gap (confirmed the same way — `callcenter_config tier add` fixes it directly, live, exactly like `agent add` does), but no code path in this task's own scope ever calls it; tier assignment is the queue's own provisioning concern, not the agent-login feature code's, so this task did not decide where that call belongs (`handleQueueDial`'s own reload trigger is the most likely spot). This gap is very plausibly the same reason distribution to a genuinely `Available`/tiered-looking agent was seen to never happen in earlier live testing before this root cause was understood — a `tier add` call for the same agent+queue, done manually via `fs_cli`, was confirmed live to succeed the same way `agent add` does — but this task ran out of time to verify actual distribution end-to-end with both fixes applied together (an unrelated SIP-registration/digest-nonce flake in the ad hoc harness used to check this, not `mod_callcenter`, blocked the final confirmation). `call.queue.caller_joined`/`.caller_left`/ESL `CC-*` headers remain entirely unexercised. | **Narrowed further, still not closed**: the wire-format risk this entry originally flagged is fully retired, and what looked like a possible deep FreeSWITCH-internals mystery turned out to have a concrete, fixable, config-shaped explanation (agent lazy-load) with a strong likely twin (tier lazy-load). **Owner to decide**: (a) where the `tier add` call belongs — most likely `handleQueueDial`'s own reload trigger, called once per queue-node pairing the same way the affinity lease already is; (b) whether to re-attempt the full end-to-end distribution proof once that's wired up, since this task got close but not all the way there. |
| **G-48** | ~~`mod_valet_parking`'s real wire shape ... unconfirmed~~ **Call shape fixed and confirmed live in S2-20**; `valet_parking.conf`'s own XML and "return-on-timeout" remain unimplemented | S2-20 gave this its first live FreeSWITCH process and found the originally-shipped `valet_park(lotname/ext)` (one slash-joined string) genuinely wrong: `mod_valet_parking.c:757`'s own usage message (`Usage: <lotname> <extension>|[ask ...]`) rejected it outright — the real syntax is two space-separated arguments, `valet_park <lotname> <extension>`, confirmed against the module's own public dialplan examples (`<action application="valet_park" data="mylot 5555"/>`). A second, subtler bug: with no `valet_hold_music` set, the parked channel's own hold-loop errored out and returned within ~0.5s instead of holding — set explicitly to the module's own `silence` sentinel (a synthetic, always-resolvable stream, `silence_stream://-1`) now. Both fixed in `buildParkDialplanDocument` (`xml.ts`). Verified end to end (`tests/sip/test/parking.test.ts`): one call dials a slot to park, a second call dials the same slot and gets bridged to real (silent) media — proof `mod_valet_parking`'s own state machine really does tell park and retrieve apart on a live node, run 3 times cleanly. Still unimplemented, not attempted by S2-20: (1) `/fs/configuration`'s `valet_parking.conf` binding still returns not-found — a lot's behavior travels inline in the dialplan action, and whether the module needs a config file beyond that remains unconfirmed; (2) a lot's own `timeout_seconds`/`return_destination_type`/`_id` still never reach the dialplan action — the longer `lotname/ext/timeout/return-ext` argument form's exact syntax is still unconfirmed, so a parked call that times out is left to the module's own undocumented-here default; (3) 03 §3.2's own dedicated `internal-app` context is still not built, same reasoning as before (nothing originates/transfers calls yet). | **Narrowed, not closed**: the call-shape risk this entry originally flagged is retired. **Owner to decide**: whether return-on-timeout is worth a follow-up task now that the base argument syntax is confirmed, or whether `mod_valet_parking`'s own default (ringing the slot back to whoever parked it) is acceptable permanently. |
| **G-49** | 06's full cross-resource numbering-plan check ("no collisions with feature codes, parking slots, conference numbers, or queue numbers") remains unimplemented, now across four resource kinds instead of one (`domain/numbering.ts`) | S1-09 shipped only extension-vs-extension collision checking, with a comment saying the rest would follow once parking lots/conference rooms/queues had an owning task. S2-13/14/15 each since landed, but each only checks collisions against its *own* kind — a queue has no dialable number to collide with anything (only reached via DID or flow), a parking lot's slot range is checked against other lots' ranges (`assertNoSlotOverlap`), and a conference room's number is checked against other rooms' numbers via a DB-level `(tenant_id, number)` unique index (`conference-room.repo.ts`'s `isDuplicateKeyError` catch, not a manual scan — a room's number, unlike a lot's slot range, is a single value a unique index can enforce directly) — none of the three cross-checks against extension numbers or each other, and extension creation/renumbering still only checks against other extensions. In practice this means an admin *can* provision an extension "701" and a parking lot spanning 700-719, or a conference room numbered the same as an extension, and nothing here will stop them — whichever this service's own `/fs/dialplan` handler checks first, in the order this file's own branches are written (extension, then conference room, then parking-lot range, then outbound), wins the collision silently. | **Owner to decide**: whether this is worth a dedicated task (a real, indexed numbering-plan table spanning all four kinds) or whether per-kind uniqueness plus documented lookup order is an acceptable v1 answer, given how unlikely an admin is to provision genuinely overlapping ranges/numbers by accident within one tenant. |
| **G-50** | ~~`mod_conference`'s real wire shape ... unconfirmed~~ **Call shape, config file, PIN flow, and PIN-verify auth all fixed and confirmed live in S2-20**; only the video/layout fields on a room remain unimplemented | S2-20 gave this its first live FreeSWITCH process and found two real, load-bearing bugs, both fixed. (1) The original assumption — that a bare `session:execute("conference", roomName)` would join under "whatever profile FS resolves by default" with **no config file at all** — was genuinely wrong: `mod_conference.c:2039` failed every call with "Open of conference.conf failed" before it ever got as far as resolving a profile by name. The module needs `autoload_configs/conference.conf.xml` to exist, config or not; a missing file is not the same as an unconfigured default. Fixed by adding a minimal `conference.conf.xml` defining exactly one profile, named `default` (matching the bare room name's own implicit profile resolution, still correct once a config file exists), with no entry/exit sound params since this image ships no FreeSWITCH sound package at all (same reasoning as G-48/G-47's own MOH/prompt gaps). (2) The PIN-collection prompts `conference.lua` played (`conference/conf-pin.wav`, `conference/conf-bad-pin.wav`) were a real bug, not just an unverified guess: with no sound package, `playAndGetDigits` hit a missing-file error and returned instantly on every one of its own internal tries, without ever actually listening for DTMF — a PIN-required room hung up on *every* caller immediately, right PIN or wrong, not merely a rejected wrong one. Fixed the same way G-48 already fixed `valet_hold_music` for the identical underlying cause: `silence_stream://1000` in place of both file paths. Verified end to end (`tests/sip/test/conference.test.ts`, run clean 4/4 after both fixes): three SIPp participants dial the same room and all join; a second scenario sends three genuinely-wrong PINs over real RFC 4733 DTMF and confirms FS hangs up only after all three are rejected by `/verify-pin`. Building that second scenario surfaced a third, SIPp-side (not product-side) bug worth recording for whoever writes the next DTMF-sending scenario in this suite: this image's `ctaloi/sipp` (v3.5.1, built 2017) has no `play_dtmf` support at all, only `play_pcap_audio` against its own bundled static per-digit pcap files — and replaying the *identical* pcap twice in a row within one call sends the identical baked-in RTP sequence number/timestamp twice, which FS's own RTP layer silently drops as a duplicate rather than registering as a second keypress. `uac_call_wrong_pin.xml`'s own doc comment has the full detail; the fix there is simply never repeating a digit back-to-back. Still unimplemented, not attempted by S2-20, same scope cut as G-48/G-47: (1) `/fs/configuration`'s `conference.conf` binding still returns not-found — a room's behavior travels inline in the dialplan action instead; (2) `conference_rooms.video`/`.layout` still never reach the dialplan action or any FS-side config — `mod_vpx` is not in this image's module set, so `video: true` behaves identically to `false` until S6-01. A third bug, found *after* this entry originally shipped (same S2-20 pass, while debugging `voicemail.lua`'s identical `httpCall`): `verify-pin`'s own auth was completely broken the whole time — `shell_exec` (the original way of base64-encoding its Basic auth header) is not a registered FS API command in this image, and even fixed, `mod_curl`'s own `curl` API command cannot carry a header value containing a space (`Basic <token>` always has one) at all — full detail in G-41. This went uncaught by the "run clean 4/4" wrong-PIN test above because a broken auth header and a genuinely wrong PIN are indistinguishable from the caller's side — both always get rejected. Fixed the same way G-41 fixes it: a single `X-Fs-Node-Token: <token>` header in place of Basic auth. Re-verified afterward with a genuinely *correct* PIN too (`tests/sip/test/conference.test.ts`'s "joins a PIN-required room with the correct PIN") — a real join, held, then the caller's own BYE, not FS hanging up on it. | **Narrowed, not closed**: the call-shape, config-file, and PIN-flow risk this entry originally flagged is all retired — a PIN-required room is now a genuinely working feature (both accept-correct-PIN and reject-wrong-PIN confirmed live), not just a documented gap. **Owner to decide**: whether video support is worth a follow-up task once S6-01 lands `mod_vpx`. |
| **G-51** | ~~`mod_json_cdr`'s real wire shape ... unconfirmed~~ **Mostly confirmed live in S2-19**; several CDR v1 fields still have no extraction logic behind them | S2-19's own two-node compose stack gave this its first real FreeSWITCH process, and most of this entry's original uncertainty is now resolved: `mod_json_cdr.conf.xml`'s `url`/`cred`/`log-b-leg`/`log-http-and-disk`/`encode-values`/`retries`/`delay`/`timeout` params all load without error, `log-b-leg=false` genuinely produces exactly one CDR per call (confirmed by running the full `tests/sip` suite and counting rows — no duplicates), and `domain/cdr.ts`'s flat top-level `variables` object reading is correct. One real bug this surfaced and S2-19 fixes: `vars.xml`'s `cuc_node_id` is a *global* variable (`$${cuc_node_id}` substitution, resolved at XML-parse time) — it is never a *channel* variable the way an explicit `<action application="set">` makes one, so `mod_json_cdr`'s own `variables` dump never included it, and every CDR was rejected (`InvalidCdrPayloadError`) until `xml.ts`'s `tenantIdAction` (S2-18) and `buildFlowDialplanDocument`'s own `vars` array were extended to set it explicitly, same as `cuc_tenant_id`. Also confirmed live and left as expected behavior, not a bug: a call that matches no dialplan `<extension>` at all (an intentionally-rejected call — a 403, a blocked international number, a channel-limit rejection) never executes any `<action>`, so none of this platform's own attribution variables are ever set on it; `mod_json_cdr` still fires a CDR for that channel, and cdr-service correctly discards it (there is no tenant to attribute an unrouted call to) rather than guessing. Still genuinely unverified/unimplemented: `cdrs.queue_id`, `.flow_id`, and `.extension_ids` have no channel-variable plumbing feeding them at all (no dialplan builder sets a `cuc_queue_id`/`cuc_flow_id` var, and resolving a calling/called extension's platform id from its SIP username would need a further pbx-config-service lookup neither task added); `legs`/`sip` are populated best-effort with no confirmed shape. | **Narrowed, not closed**: the wire-format risk this entry originally flagged is retired. **Owner to decide**: whether `queue_id`/`flow_id`/`extension_ids` attribution is worth a follow-up task (the same explicit-channel-variable pattern `cuc_tenant_id`/`cuc_node_id` now establish) or an acceptable permanent gap given a CDR is still fully usable for billing/disposition purposes without them. |
| **G-52** | `cdrs` partition maintenance and retention have no owning task (S2-18) | The same gap G-12 already documents for `audit_events`, now also true for `cdrs`: `002_add_cdrs.ts`'s migration creates monthly `RANGE COLUMNS` partitions on `start_at` for a multi-year window generated at migration-write time, plus a `MAXVALUE` catch-all so inserts never fail — but nothing rolls the window forward or drops/archives partitions past whatever retention a future task defines. Every row still lands somewhere (the catch-all), so this is an efficiency/retention gap, not a correctness one — but CDRs are billing-relevant, so retention here is a real requirement eventually, not just disk hygiene. | **Owner to decide**: schedule a periodic job (most naturally alongside G-12's own audit-retention job and S5's broader retention-policy work) that adds a partition for each upcoming month and drops/archives partitions past whatever retention period billing/compliance needs settle on for CDRs specifically (likely longer than audit's own default 1 year). |
| **G-53** | The master-level billing rollup C-1's own answer #3 describes is not built (S2-18) | Issue #95's own resolution says "the master exposes all CDRs for all call types and number of extensions (broken down per type) for each tenant account" — but no concrete API shape for that rollup is specified anywhere (not in 06-services.md, not in the issue itself beyond that one sentence), so `billing.routes.ts` only builds the per-tenant `GET /v1/tenants/{t}/billing-records` list C-1's own column table describes, reachable by a reseller (ordinary org-ancestry, no H1 block on `usage`-class data) or the master. | **Owner to decide**: what the master's own rollup view should actually look like (a new endpoint, a query-string toggle on `billing-records`, a separate aggregate resource) before it can be built — 06-services.md's own cdr-service section is the natural place to record that shape once decided. |
| **G-54** | No merged OpenAPI document exists for the console client to be generated from (S3-01) | 08 §1 says the `console_api` client is generated from "the merged OpenAPI spec", but no task produces one: api-gateway serves no `/openapi.json` of its own and proxies only `identity` and `org`, and each service's document is only reachable from a running instance (it needs its database). S3-01 therefore generates from a hand-kept seed, `apps/console/api/openapi.yaml`, covering only `GET /v1/public/brand` and `POST /v1/auth/login`, and ships `apps/console/tool/merge-openapi.mjs` to union per-service documents once a source for them exists. The seed models the server's two `oneOf` responses as single objects with optional fields (wire-compatible), so it can drift from the services' TypeBox schemas with nothing to catch it. | **Owner to decide**: who produces the merged spec — a gateway-served `/openapi.json` that merges its upstreams, or a build step that dumps each service's document without a database — and a contract check that fails when the seed and the services disagree (09 §Contract tests). Needed before S3-04, which is the first screen that calls more than these two routes. |
| **G-55** | The password-reset and invitation events carry the one-time token (S3-04) | To email a reset or invitation link, notification-service needs the token, and the only channel between the two services is the event bus. So `identity.user.password_reset_requested` and `identity.invitation.created` carry it in their payload. That puts a live credential in the outbox table and in the JetStream stream for as long as either retains it; the token is single-use and short-lived (an hour, seven days), and only its SHA-256 is stored by identity-service itself, but the bus copy is plaintext. Alternatives: encrypt those two payloads for the mailer, or send an event without the token and have notification-service call identity-service at send time to mint the link. | **Owner to decide** before either flow is used with real users: accept the short-lived plaintext token on the bus, or choose one of the alternatives. `DEV_EXPOSE_TOKENS` (default off) logs tokens for local use when no mailer is running; it must never be on in a real deployment. |
| **G-56** | Login needs a typed organization id; invitations are own-org only; new users have no role (S3-04) | The gateway does not resolve an org from the hostname, so the sign-in, reset, and invitation-accept pages ask for an organization id (an invitation link fills it in). Inviting is limited to the caller's own org because identity-service holds no org read model and cannot tell what type of org another id names; a reseller inviting a tenant's first user still goes through org-service's admin-user creation. Users created by an invitation get no role assignment, and access tokens carry empty `roles` and `perms`, so nothing yet grants a user anything beyond the org-type hard rules. | **Owner to decide**: how the org is resolved at sign-in (tenant subdomain, or an org lookup by slug on the public brand route), where an invitation names its role, and when per-permission checks replace the route-level hard rules. |
| **G-57** | Emails go out from the platform address, not the reseller's (S3-03) | Plan S3-03 says the sender is "the brand's verified address or `PLATFORM_NOREPLY_ADDRESS`". Nothing in org-service records whether a reseller's `email_from_address` domain passes SPF and DKIM (02 §5.4 says it must before use), so there is no "verified" to test. notification-service therefore always sends from `PLATFORM_NOREPLY_ADDRESS` and uses the reseller's `emailFromName` (or display name) as the sender's display name; the neutral presentation has no display name. Links in the email point at the reseller's first console hostname, or `https://console.$PLATFORM_BASE_DOMAIN`. Also not built: the plan's third template, MFA reset, has no event or flow that asks for it (no MFA-reset route exists in identity-service). | **Owner to decide**: how a reseller's sending domain is verified and recorded, so its own address can be used; and whether an MFA reset flow (who can trigger it) is in scope, which would add its template. |
| **G-58** | The console re-themes after login from a session brand route (S3-02) | `GET /v1/session/brand` answers for the signed-in actor's own org: a tenant gets its reseller's brand, a reseller its own, the master neutral. It needed a permission every signed-in actor has, and 07 §3.3 has none, so `org.view` (config class) is added to the catalog and to every built-in role. A custom role does not get it automatically, so a user whose role lacks it keeps the hostname's brand. | **Owner to decide**: whether 07 §3.3 should carry `org.view` (or a broader read set, see the gap noted for support roles), and whether custom roles should always include it. |
| **G-59** | ~~The call-flow builder cannot pick a schedule, and its node types are hand-written~~ **Resolved** (time_condition references a schedule; S3-10) | `time_condition` config is now `{ scheduleId }` (the time zone is gone), and the builder offers a schedule picker. **The schedule is evaluated on every call, not baked into the published IR:** the runner asks telephony-config (`GET /fs/flow/:tenantId/schedule/:scheduleId/open`), which asks pbx-config-service (`GET /internal/v1/tenants/:tenantId/schedules/:id/open`, service-token authenticated), which owns the schedule and evaluates it in its own IANA time zone against its weekly windows and holidays. So editing hours or a holiday takes effect on the next call with no republish, the FreeSWITCH node stays stateless (rule 5) and never does time zone arithmetic, and there is no cache to go stale. Embedding the resolved schedule in the IR at publish time was rejected because published versions are immutable and it would freeze the hours. A schedule that no longer exists, or a service that cannot be reached, takes the `noMatch` (closed) branch and is logged, so a call nobody can vouch is inside hours gets after-hours handling. **Flows that already have a `time_condition`:** a published version is immutable and keeps its `{ timezone }` IR; the runner takes `match` for such a node, as it always did, and logs a warning. A draft saved that way fails `:validate` and `:publish` with an `invalid_config` issue on the node ("was saved with a time zone and no schedule. Choose a schedule for it.") rather than compiling silently, and the builder shows the same problem locally. The other half of the original question, hand-written node types held to the IR by test against `api/callflow-ir.json` instead of generated from JSON Schema, stands as built. | **Resolved**: the owner chose a schedule reference on 2026-09-24. |
| **G-60** | The gateway routes only identity and org, so most of the console cannot work through it (S3-11) | Walking the pilot journey through the gateway showed that `ROUTE_TABLE` accepts only the services `identity` and `org` (`KNOWN_SERVICES`), and sends every `/v1/tenants/*` path to org-service. So `/v1/tenants/:id/extensions`, `/dids`, `/flows`, `/media-assets` and the rest of what the console's PBX screens and call-flow builder call (pbx-config-service, callflow-service, voicemail-service, trunk-service, cdr-service) have no route through the gateway in any deployment; those screens only work against the demo backend. Fixed here: `/v1/session/brand` (added in S3-02) had no route and returned 404, so the re-theme after login never worked through the gateway; it now goes to org-service. Also fixed: callflow-service could not start with `TRUST_INTERNAL_HEADERS=true` because it neither read nor passed `INTERNAL_HEADER_SIGNING_SECRET`. **Not fixed**: routing by the segment after the tenant id (`/v1/tenants/:id/<resource>`) needs the gateway to route by pattern, not prefix, and to know the five more services. `tests/e2e` therefore calls callflow-service directly with signed headers and leaves trunk, extension, DID and CDR steps as `todo`. | **Owner to decide**: the gateway routing model (pattern routes and a service registry), and who owns it before M2. Until then the M2 walkthrough cannot pass. |
| **G-61** | ~~The org comes from the console hostname; the typed organization ID is a fallback (G-56 follow-up)~~ **Resolved** (users-org-ancestry) | identity-service asks org-service (internal `GET /internal/v1/hosts/:host/sign-in-scope`, service token) which org a hostname belongs to: `console.{PLATFORM_BASE_DOMAIN}` is the master, a registered reseller console hostname is that reseller. The gateway forwards the browser's `Host` as `x-forwarded-host` on `/v1/auth` (overwriting anything the client sent). Sign-in and reset then need no organization: at a reseller's hostname the reseller's own users **and its tenants' users** (matched on `users.reseller_id`) are candidates by email, and the master hostname reaches only master users. A tenant user therefore picks their org by email, not by a tenant subdomain: tenant domains are SIP domains (02 §3), not console hostnames. If one email is in several tenants under the reseller, the password tells them apart (each candidate is checked, capped at 25); if the same email and password fit two, the answer is a 409 `org_required` and the page then shows the Organization ID field; a hostname nobody owns (a local address) answers 400 `org_required` the same way. That depends only on the address the page is served from, and the 409 only appears after the password matched, so neither says who has an account. A reset for an address in several tenants emails a link for each, and always answers 202. The browser never asks a public route to map hostnames to org ids (there is none), and `ORG_SERVICE_URL` is now required by identity-service. It assumes the console and the API share a hostname (the refresh cookie is `SameSite=Strict` on `/v1/auth`). **Resolved:** keep email lookup inside the reseller's console. Because the password is what tells two accounts apart, creating one that could not be told apart is now refused: accepting an invitation, confirming a password reset, and creating an org admin answer 409 `password_in_use` when the chosen password already opens another active account with the same email under the same sign-in scope (the reseller and all its tenants; the master alone). It fires only when the password already opens the other account, so it says nothing a sign-in would not, and a reset link is not spent by a refusal. The console explains it on the invitation and reset pages. Not done: an account that is re-enabled after being disabled can still end up sharing a password with an active one, and sign-in then asks for the organization ID. | Resolved. |
| **G-62** | ~~Users: own organization only, built-in roles only, disable instead of delete (S3-08)~~ **Resolved** (users-org-ancestry) | The Users screen lists, renames, disables and enables the people in the signed-in user's own organization (`/v1/orgs/{orgId}/users`); a reseller or master acting as a tenant cannot manage that tenant's people, for the same reason invitations are own-org only (G-56: identity-service has no org read model to check ancestry against). People arrive by invitation and get one of the built-in roles for their kind of org through the existing role assignment routes; custom roles are not offered. A user cannot be deleted, only disabled (which revokes every session and publishes `identity.user.updated`), so audit records keep pointing at someone; nobody can disable themselves. Schedules (`/v1/tenants/{t}/schedules`, pbx-config-service) are weekly open windows in a schedule's own time zone plus holiday dates, validated by the service; nothing consumes them yet (see G-59). **Resolved:** a reseller manages its own tenants' people and the master manages anyone's. identity-service asks org-service for where an org sits (internal `GET /internal/v1/orgs/:id/lineage`, cached in memory because orgs never move; a missing org is not cached) instead of keeping an org read model fed by events: events carry no org type for the master, and an org created before a consumer existed would be missing. The check (`authz/org-access.ts`) covers the users list and edit routes, invitations, role listing and creation, and role assignments (which also now require the user to belong to the org, closing a gap where any org id was accepted). A tenant reaches only its own org and never triggers a lookup; a reseller's own org works while org-service is down; an org that does not exist is refused exactly like one the actor may not reach (403 `users_other_org`), and an unreachable org-service is a 503. The console's Users screen follows "act as" (and "People" on a tenant): a reseller or the master manages the tenant they are in. Built-in roles only and disable-only, as decided. | Resolved. |
| **G-80** | Browser uploads go straight to storage, which has no CORS rule yet (S3-08) | The media screen uploads a recording the way 05 §4 says: the service hands out a presigned PUT address, and the browser sends the bytes there with a plain request (no bearer token, no cookies), then asks the service to finalize. That is a cross-origin request from the console to the bucket, and nothing in `@cuc/storage` (`provisionBucket`) or the compose MinIO sets a CORS rule, so a real browser will refuse the PUT until the bucket allows the console's origin, `PUT`, and the `Content-Type` header. This was tested against the demo backend and the service's own presign test, not in a browser against real storage. There is also no playback of a stored recording in the console: the service has no route that returns a download address for an asset. | **Owner to decide**: how buckets accept browser uploads: a CORS rule on each bucket (allowed origins would be every reseller's console hostname, so likely any origin, safe only because each address is short-lived and signed), or a same-origin upload path through the edge; and whether to add a presigned download route so recordings can be played back. |
| **G-100** | Resetting a user's two-step verification (S3-03, S3-04) | `POST /v1/orgs/{orgId}/users/{userId}/mfa-reset` (`user.manage`, config class) lets an admin remove a user's authenticator for a lost phone. Reach follows G-62: own org, the master anywhere, a reseller its tenants. In one transaction it deletes the user's factors, clears `mfa_enrolled`, revokes every session, and writes the outbox events `identity.user.mfa_reset` (email and display name, no secret) and an `audit.event.recorded` (`user.mfa_reset`, target org, config class). Nobody resets their own (409 `cannot_reset_self`: it would end the session in use, and a lost phone is another admin's job), and a user who never enrolled is a 409 `mfa_not_enrolled` with nothing recorded. Nothing else is needed for the user to recover: master and reseller users are asked to enroll at their next sign-in exactly as on a first login (a tenant user is not asked for a second factor at all, so a reset only removes theirs). notification-service sends the third template, `mfa-reset`: it says what happened, links to the console's sign-in page (no token, so it is worth sending however late), is branded like the other two or neutral for the master, and still goes from `PLATFORM_NOREPLY_ADDRESS` (G-57 is unchanged and still open). Access tokens already issued live out their short lifetime; only refresh is cut. | **Owner to decide**: whether a reset should also need a second admin's approval, or a cooling-off delay, for master and reseller accounts; and whether the user should be told at once by another channel (SMS) as well as email. |
| **G-101** | ~~`http_cache://` media URLs had no file extension, so no prompt could ever play from a call flow~~ **Resolved** (S3-11, first live call through `flow_runner.lua`) | `mod_http_cache` keeps the URL's own extension on the file it writes, and FreeSWITCH picks a file format from that extension. The URLs built for FS ended in `/8k`, so the download succeeded and then `Failed to open HTTP cache file` — a `menu` node's `playAndGetDigits` returned nothing and the menu took its `timeout` port within 20 ms of answering, every time. It was invisible before because `media_playback.test.ts` only asserted that the call was answered and held, which happens either way. Fixed: `/fs/media/:tenantId/:assetId/:rate` also accepts `8k.wav` and `16k.wav` (the bare forms still work), and the URL builders (`mediaUrl` in `fs.routes.ts`, `flow_runner.lua`) now emit `8k.wav`. Proven live by `tests/sip/test/call_flow.test.ts`: a carrier call to a DID bound to a published flow reaches its `voicemail` node and records a message, and a second call presses 2 (RFC 4733 DTMF) at a `menu` and is taken to voicemail. **Still not proven live:** the other flow nodes (`play`, `time_condition`, `extension`, `ring_group`, `queue`, `goto_flow`), and that the prompt is audible — SIPp receives RTP but nothing asserts its content. Note for scenario authors: a caller that must press digits has to offer `telephone-event/101` in its SDP, or FS never negotiates RFC 4733 (`trunk_invite_press_2.xml`). | None: resolved. |
| **G-102** | Where a phone registers is told by pbx-config-service, from the tenant's domain and two settings (SIP endpoint setup) | The console's "Connect a phone" panel needs a server, port and transport as well as an extension's login. `GET /v1/tenants/{t}/sip-endpoint` (pbx-config-service, `extension.manage`, config class) returns them. **Server and realm** are the tenant's primary domain from org-service, the same value the extension's digest hashes use, so what a phone is told and what it authenticates against cannot differ; a tenant with no domain gets a 409 rather than a made-up address. **Port and transports** are the edge's own and come from `SIP_PUBLIC_PORT` (default 5060) and `SIP_PUBLIC_TRANSPORTS` (default `udp,tcp`, the two OpenSIPs listens on today). The **password** is still write-only outside the audited `secret.reveal` action; the panel asks for a reason and sends it with the reveal. **Password reset:** `POST /v1/tenants/{t}/extensions/{id}/reset-password` (`secret.reveal`, secret class, audited as `extension.credential.reset` with the reason) generates a new random password, stores it encrypted with new digests, and queues `pbx.extension.updated` so telephony-config re-projects the OpenSIPs subscriber. It returns the new password once, so it needs the same permission as reveal; the old password stops working when OpenSIPs picks the change up. **Not built:** TLS or SRTP (the edge has no TLS listener, so `tls` is accepted in the setting but nothing serves it) and desk-phone auto-provisioning (a provisioning URL per phone model). If the SIP hostname should ever differ from the tenant's domain (a shared edge name, SRV records), the server field needs its own source. | **Owner to decide**: whether desk-phone auto-provisioning is wanted for M2, and whether TLS is required before pilot. |
| **G-103** | Yealink desk phones set themselves up from the platform (auto provisioning) | A tenant adds a phone by MAC address and picks the extension it registers as (`/v1/tenants/{t}/devices`, pbx-config-service, `extension.manage`). **The MAC is unique across all tenants**, because a phone asks for `<mac>.cfg` and one address belonging to two tenants would be two answers; a clash gets the same 409 whichever tenant holds it. **Setup credentials:** `POST .../devices/{id}/provisioning-credentials` (`secret.reveal`, secret class, audited as `device.provisioning.issued`) issues a random password for that device. Only its SHA-256 is stored and it is shown once; issuing again replaces it. The phone authenticates with HTTP Basic (device id and that password). They are kept out of the URL path because request logs record paths and not headers, and the gateway forwards `Authorization` for that one prefix only. **The route:** `GET /v1/public/provision/yealink/{file}` is public at the route contract and authenticates itself; every failure is the same 401 with a `Basic realm="provisioning"` challenge, whether the device exists or not. `<mac>.cfg` is served only when the MAC matches the authenticated device; the model-wide `y0000000000NN.cfg` gets a valid empty file. The device is found by id before the tenant is known, so that one lookup is deliberately unscoped and everything after it is scoped to the tenant it returns. **The file** sets account 1 (user and auth name, password, server, port, transport as `SIP_PUBLIC_*` says) and asks the phone to fetch again every 1440 minutes, so a password reset or moved extension reaches it without a reboot. Provisioning URLs are built from `PROVISIONING_BASE_URL`; unset, they are reported as null. **Risks to know:** the file necessarily carries the extension's SIP password in clear, so the public base URL should be HTTPS. **Not verified against a real phone:** the key names follow Yealink's current (firmware 80 and later) format and older firmware uses different ones. **Not built:** other vendors, more than one line per phone, line and BLF keys, time zone, language and firmware settings, Yealink's redirection service, and DHCP option 66 setup on our side (the console shows the value to configure). **Decided:** phones reach provisioning through the api-gateway's own public address, not a dedicated host, so `PROVISIONING_BASE_URL` is that address (the compose stack defaults it to `http://localhost:8080`). **Consequence to watch:** the gateway limits each client IP to 300 requests a minute (`RATE_LIMIT_IP_MAX`), and a phone costs about three or four requests when it boots. A site of roughly a hundred phones behind one public address that all reboot together, after a power cut, would be slowed by 429s; phones retry, so it is a delay, not a failure. Raising the limit for the provisioning prefix is the fix if it shows up. **Platform-wide credential (owner decision):** the provisioning server also requires a global user name and password over HTTP Basic (`PROVISIONING_USERNAME`, `PROVISIONING_PASSWORD`, set together). Any phone that presents them can fetch the file named for its own MAC address, with nothing set up for that phone first; the per-device credentials above still work and open only that device's file. **Risk, stated plainly:** this credential keeps outsiders off the server, but it does **not** keep one tenant's phones from another's. A MAC address is not a secret, and every tenant's phones (and so every tenant admin who can read a phone's settings page) end up holding this password, so anyone holding it who knows or guesses a registered MAC can fetch that phone's file, which contains its extension's SIP password. An unregistered MAC gets the same 404 as any missing file, but a registered one answers 200, so the credential also lets its holder confirm which MACs are registered. The gateway's per-IP limit (300 a minute) slows guessing but does not stop it. What limits the damage: the credential is never shown in the console, the per-device credential is the isolated alternative, and a reset changes the SIP password the file carries. If cross-tenant isolation matters, use per-device credentials only by leaving these two unset. | **Owner to decide**: whether to support more vendors before pilot, and whether the platform-wide credential should stay once phones are set up per device. |
| **G-104** | The edge speaks HTTPS, and phone provisioning is never served without it (owner decision: secure the console and the provisioning server) | **What is built.** api-gateway serves HTTPS when given a certificate: `TLS_CERT_FILE` and `TLS_KEY_FILE` (both or neither) are the default certificate, and `TLS_CERT_DIR` holds a certificate per hostname chosen by the name the client asks for (SNI): `<dir>/<hostname>/fullchain.pem` and `privkey.pem`, with a wildcard in `_.<domain>`. Whatever issues and renews them (an ACME client, per 02 §3) only writes files there; a renewed file is picked up within a minute without a restart. TLS 1.2 is the floor. A name a client sends can never reach outside that directory. **Headers on every response:** `X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy: no-referrer`, `Cross-Origin-Opener-Policy`, a restrictive `Permissions-Policy`, and, when the request arrived over HTTPS (directly or as a trusted proxy says), `Strict-Transport-Security`. API responses are `Cache-Control: no-store`, which matters most for provisioning. `HTTP_REDIRECT_PORT` adds a plain-HTTP listener that answers 308 to the HTTPS address. **Provisioning refuses plain HTTP** (`REQUIRE_HTTPS_FOR_PROVISIONING`, on by default): the file holds the extension's SIP password and the request holds the platform credential, so a phone told an `http://` address gets a 403 explaining why, not an insecure success. Development over `http://localhost` turns it off. **Not built here:** certificate issuance and renewal (ACME) itself, which is the production topology work (S4-01, S4-11); `console_hostnames.tls_status` is still never updated. **Console hosting:** with `CONSOLE_DIR` set, the gateway serves the built console from its own origin (which the refresh cookie's `SameSite=Strict` needs), under a strict Content-Security-Policy: everything from its own origin, nothing may frame it, `wasm-unsafe-eval` for CanvasKit and inline styles for the loading spinner, plus any origin in `CONSOLE_CONNECT_SOURCES` (the object store media uploads go to, G-80). Unknown paths without an extension get `index.html`, since the console uses path URLs; files are checked against the directory's real path, so neither `..` nor a symlink escapes it. Build with `flutter build web --release --no-web-resources-cdn` (CI now does) so CanvasKit is bundled. Checked in a real browser: the sign-in page renders with no policy violation and no request to any outside origin. **One consequence:** Flutter's fallback fonts for rare characters are fetched from Google at run time, and the policy blocks that, so a character outside the bundled fonts shows as a placeholder box. Bundling a fallback font fixes it. SRTP (encrypted media) is separate and not built. | **Owner to decide**: which ACME client and CA issue certificates, and who runs it. |
