<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  S2-20 (G-47): `trunk_invite.xml` verbatim, except the pause before BYE is
  widened from 500ms to 5s — the same "same scenario, longer hold" relationship
  `uac_call_hold.xml` already has to `uac_call.xml` (see that scenario's own
  doc comment), for an analogous reason: a call landing in a `mod_callcenter`
  queue needs real wall-clock time for FS's own agent-dispatch thread to
  actually offer the call to a ready agent before hanging up, not just
  time for the queue app to answer and enqueue the caller. Confirmed
  live: `trunk_invite.xml`'s original 500ms was long enough for the queue
  to enqueue the caller (a real `callcenter_config queue list members` row,
  state `Abandoned`) but not long enough for the agent to ever be offered
  the call at all (`last_offered_call`/`no_answer_count` both stayed 0) —
  not a distribution bug, a test-timing one.

  `[field0]`=caller user (the carrier's own caller-ID), `[field1]`=the
  R-URI/To domain (OpenSIPs' own SIP identity), `[field2]`=the dialed DID,
  in E.164 — same as `trunk_invite.xml`.
-->
<scenario name="Trunk INVITE to a DID, held longer before hangup">
  <pause milliseconds="6000"/>

  <send retrans="4000">
    <![CDATA[
      INVITE sip:[field2]@[field1] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      From: <sip:[field0]@[field1]>;tag=[call_number]
      To: <sip:[field2]@[field1]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:[field0]@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=- [call_number] [call_number] IN IP4 [local_ip]
      s=-
      c=IN IP4 [local_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true">
  </recv>

  <recv response="180" optional="true">
  </recv>

  <recv response="200" rtd="true">
    <action>
      <ereg regexp="sip:[^&gt;]*" search_in="hdr" header="Contact:" assign_to="1"/>
    </action>
  </recv>

  <send>
    <![CDATA[
      ACK [$1] SIP/2.0
      [last_Via:]
      From: <sip:[field0]@[field1]>;tag=[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:[field0]@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <pause milliseconds="5000"/>

  <send retrans="500">
    <![CDATA[
      BYE [$1] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      From: <sip:[field0]@[field1]>;tag=[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" crlf="true">
  </recv>

</scenario>
