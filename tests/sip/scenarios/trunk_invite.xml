<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  S2-03: the caller side of "a carrier trunk sends an INVITE to a DID, it
  rings the bound extension, answered, BYE". Unlike `uac_call.xml`, there is
  no REGISTER preamble at all — a trunk is identified by source IP
  (`opensips.cfg.template`'s `check_source_address`), never by registering,
  so this scenario's very first message is the INVITE itself, exactly the
  shape a real carrier's own signaling would take.

  `[field0]`=caller user (the carrier's own caller-ID, arbitrary), `[field1]`=
  the R-URI/To domain (OpenSIPs' own SIP identity — a carrier addresses its
  trunk termination point, not any tenant's internal domain, which it has no
  reason to know), `[field2]`=the dialed DID, in E.164.

  Reuses `uac_call.xml`'s own hard-won INVITE/ACK/BYE shape verbatim (same
  Call-ID-reuse rationale, same post-`topology_hiding()` Contact-targeting
  for in-dialog requests) — see that scenario's own doc comment for why.

  The leading `<pause>` gives `run-scenario.ts`'s `startDelayedCaller` a real
  window to read this container's own IP back out and provision (and wait
  for telephony-config to project) a trunk/DID whitelisting it *before* the
  INVITE below ever fires — see that function's own doc comment.
-->
<scenario name="Trunk INVITE to a DID">
  <pause milliseconds="6000"/>

  <send retrans="4000">
    <![CDATA[
      INVITE sip:[field2]@[field1] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      From: <sip:[field0]@[field1]>;tag=[call_number]
      To: <sip:[field2]@[field1]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:[field0]@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=- [call_number] [call_number] IN IP4 [local_ip]
      s=-
      c=IN IP4 [local_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true">
  </recv>

  <recv response="180" optional="true">
  </recv>

  <recv response="200" rtd="true">
    <action>
      <ereg regexp="sip:[^&gt;]*" search_in="hdr" header="Contact:" assign_to="1"/>
    </action>
  </recv>

  <send>
    <![CDATA[
      ACK [$1] SIP/2.0
      [last_Via:]
      From: <sip:[field0]@[field1]>;tag=[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:[field0]@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <pause milliseconds="500"/>

  <send retrans="500">
    <![CDATA[
      BYE [$1] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      From: <sip:[field0]@[field1]>;tag=[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" crlf="true">
  </recv>

</scenario>
