<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  S2-03: "an INVITE from an unknown IP is rejected". `route{}`'s own
  `check_source_address`/`is_from_local` both miss for a source that matches
  no trunk and no registered domain, so `opensips.cfg.template` rejects with
  403 before the call ever reaches FS (the same `else` branch S1-11's
  registered-phone routing already falls into). No ACK/BYE: the call never
  forms a dialog, and SIPp generates the ACK for a non-2xx final response to
  INVITE on its own.
-->
<scenario name="Trunk INVITE from an unrecognized source (expect 403)">
  <send retrans="4000">
    <![CDATA[
      INVITE sip:[field2]@[field1] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      From: <sip:[field0]@[field1]>;tag=[call_number]
      To: <sip:[field2]@[field1]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:[field0]@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=- [call_number] [call_number] IN IP4 [local_ip]
      s=-
      c=IN IP4 [local_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="403">
  </recv>

</scenario>
