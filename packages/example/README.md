# @cuc/example

Scaffold placeholder. It carries no product code — it exists so `pnpm lint typecheck test build`
has a workspace member to run against (task S0-01). Delete it once real packages exist.
