<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  S5-12 (G-111): a carrier call refused because its tenant requires recording
  and the recording cannot be set up. telephony-config's refusal document
  gives early media (`pre_answer`, so a 183 may arrive first) for a neutral
  tone, then hangs up with NORMAL_TEMPORARY_FAILURE. FreeSWITCH sends that as
  503, and OpenSIPs, as RFC 3261 §16.7 requires of a proxy, passes a 503 on as
  500, so the carrier sees 500. The call is never answered: a 200 here fails
  the scenario.

  Same leading pause as `trunk_invite.xml` (the trunk and DID are provisioned
  with this container's IP during it), and the same optional 100 as
  `trunk_invite_expect_404.xml` for the same reason. Unlike that scenario this
  one ACKs the final response, as a UAC should.

  `[field0]`=caller user, `[field1]`=R-URI/To domain, `[field2]`=dialed DID.
-->
<scenario name="Trunk INVITE to a DID refused with 500 after a tone">
  <pause milliseconds="6000"/>

  <send retrans="4000">
    <![CDATA[
      INVITE sip:[field2]@[field1] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      From: <sip:[field0]@[field1]>;tag=[call_number]
      To: <sip:[field2]@[field1]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:[field0]@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=- [call_number] [call_number] IN IP4 [local_ip]
      s=-
      c=IN IP4 [local_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true">
  </recv>

  <recv response="180" optional="true">
  </recv>

  <recv response="183" optional="true">
  </recv>

  <recv response="500" timeout="20000">
  </recv>

  <send>
    <![CDATA[
      ACK sip:[field2]@[field1] SIP/2.0
      [last_Via:]
      From: <sip:[field0]@[field1]>;tag=[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

</scenario>
